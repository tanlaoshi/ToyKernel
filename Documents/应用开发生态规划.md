# ToyOS 应用开发生态规划

> **本文是应用开发方向的总纲。** 后续所有应用相关 PR 都引用本文。
>
> 相关文档：
> - [`应用开发指南.md`](应用开发指南.md) — 30 分钟上手（**PR-A-doc-guide** ✅）
> - [`技术手册.md`](技术手册.md#tm-iv-libs) §IV — 开发库、GUI、字体
> - [`技术手册.md`](技术手册.md#tm-iv-gui) — 第一个 GUI 程序
> - [`User/Pkg/README.md`](../User/Pkg/README.md) — 应用模板
> - [`路线图.md`](路线图.md) — 总体排期

---

## 一、目标

让一个**会 C 语言、但不了解 ToyOS 内核**的人，能在**30 分钟内**：

1. 拿到 SDK（当前：仓库里的 `User/include` + `User/Library` + `User/Pkg`）
2. 编译第一个 Hello World
3. 在 QEMU 上运行
4. 开始写自己的应用

**核心原则**：

- 应用开发者不需要看内核代码
- 一个 SDK 目录 + 一份指南 + 几个示例 = 可以开始
- 静态库 + 无依赖 = 符合 ToyOS 哲学

---

## 二、ToyOS 现在能提供什么

### 2.1 用户态可用的组件

| 类别 | 内容 | 位置 |
|------|------|------|
| **CRT** | `crt0.S` / `syscall.S` / printf / malloc / string | `User/crt/` |
| **libc 子集** | stdio / stdlib / string / unistd / fcntl / signal / ctype / errno / dirent / sys/mman | `User/include/` |
| **libtoyos.a** | CRT + libc 打包（`TOYOS_CRT` **1.2.0**） | `User/Library/ToyOs/` |
| **libToyUi.a** | 窗口 / 标签 / 按钮（ABI **1.0.0**） | `User/Library/ToyUi/` |
| **libToyGfx.a** | 文字 damage + 像素矩形 blit（ABI **1.1.0**） | `User/Library/ToyGfx/` |
| **libToyNet.a** | socket 薄封装（ABI **1.1.0**；内核默认 `LWIP=1`） | `User/Library/ToyNet/` |
| **应用模板** | `pkg` / `pkg/gui` / `pkg/net` | `User/Pkg/` |
| **链接脚本** | `user.ld`（x86 @ `0x40000000`） | `User/` |

### 2.2 系统调用（应用可见 ABI）

**29 个 syscall（号 0–28）**，与 `Include/Syscall.h` / `User/include/toyos/syscall.h` 一致：

| 类别 | syscall |
|------|---------|
| 进程 | exit / fork / wait / execve / kill / yield / signal |
| 文件 | open / read / write / close / FileStat / OpenDirectory / ReadDirectory |
| IPC | pipe / dup |
| 内存 | brk / mmap / munmap |
| 网络 | socket / bind / connect / listen / accept（send/recv = 在 socket fd 上 write/read；需 `LWIP=1`） |
| 窗口 | create_window / damage / poll_input / ui_button / damage_rect |

CRT / libToy* 已包装上述能力；应用一般不必直接调 `toy_syscall`。

详见 [`技术手册.md`](技术手册.md#tm-i-layers) §7.1（手册表目前写到 26；**27=`signal`、28=`damage_rect` 以头文件为准**）。

---

## 三、缺口盘点

### 3.1 libc 缺口

| 头 | 已有 | 明显缺口 |
|----|------|----------|
| `<stdio.h>` | printf / sprintf / snprintf / puts / perror | **fopen / fread / fwrite / fseek**（无 FILE*） |
| `<stdlib.h>` | malloc / free / exit / atoi / atol / abs / qsort | **realloc / calloc / strtol / getenv** |
| `<string.h>` | strlen / strcmp / strncmp / strcpy / strncpy / mem* | **strstr / strchr / strdup / strcat** |
| `<unistd.h>` | read / write / close / fork / wait / pipe / dup / execve / brk / kill / mmap / munmap | **lseek / getcwd / chdir / sleep / getpid**；无 libc `yield()`（用 `toy_yield()`） |
| `<fcntl.h>` | open + 标志宏 | 内核**忽略** flags |
| `<signal.h>` | signal / kill；SIGINT / KILL / TERM；教学级自定义 handler | 无 sigaction / mask / 停止作业 |
| `<ctype.h>` | isdigit / isspace / isalpha | 极小集 |
| `<dirent.h>` | OpenDirectory / ReadDirectory / CloseDirectory / FileStat（`TOY_DIR *`，非 POSIX 名） | 够用 |
| `<sys/mman.h>` | mmap / munmap；`PROT_*` / `MAP_ANONYMOUS` | 无 `MAP_FIXED` / `mprotect`；文件映射为私有拷贝 |
| `<sys/wait.h>` | ❌ 无（`wait` 在 `<unistd.h>`） | `wait(int *status)` **不按 pid 等**；CRT 目前把 `*status` 写成 0 |
| `<math.h>` | ❌ 无 | 可选 |

### 3.2 领域库缺口

| 库 | 现状 | 缺口 |
|----|------|------|
| **libToyGfx** | `ToyGfxDamageText` + `ToyGfxDamageRect`（≤64×64 像素 blit） | **点 / 线 / 矩形封装 / 位图** |
| **libToyUi** | CreateWindow / SetLabel / AddButton / Poll | **文本输入框 / 列表 / 滚动条 / 菜单 / 复选框** |
| **libToyNet** | socket / connect / bind / listen / accept / send / recv；`ToyNetIpv4()`；`ToyNetResolve`（点分 IPv4 / localhost） | sockaddr；完整 DNS（域名请用 Shell `dns`） |
| **libFsUtil** | ❌ 无 | 路径操作 / 目录遍历封装 / 多卷前缀 |
| **libjson / libini** | ❌ 无 | 配置解析 |
| **libm** | ❌ 无 | sin / cos / sqrt |

### 3.3 缺口的优先级

| 优先级 | 缺口 | 影响 |
|--------|------|------|
| **P0** | libToyGfx 点/线/矩形封装（底层 blit 已有） | 画图仍要自己填像素缓冲 |
| **P0** | libToyUi 文本输入框 | 无法做交互式表单 |
| **P0** | lseek / fopen 系列 | 大文件处理只能按流读 |
| **P1** | realloc / calloc | 动态数组实现困难 |
| **P1** | strstr / strchr | 字符串处理繁琐 |
| **P1** | libFsUtil | 路径拼接重复代码 |
| **P2** | sockaddr 结构 | 网络编程不符合 POSIX 习惯 |
| **P2** | sleep / getpid / libc `yield()` | 无延时；进程示例偏啰嗦 |
| **P2** | `wait` 填真实退出码 | 拿不到子进程 return 值 |
| **P3** | libm | 数学计算 |
| **P3** | BMP/PNG 解码 | 图片显示 |

---

## 四、库的提供方式

### 4.1 形态选择

| 方式 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **静态库** | 简单、无依赖 | 体积大 | ✅ **采用** |
| 动态库 | 体积小、共享 | 需要动态链接器 + 版本管理 | ❌ 太复杂 |
| header-only | 零链接 | 编译慢、代码重复 | ⚠️ 仅限小库 |

**采用静态库**，符合 ToyOS「无依赖」哲学。

### 4.2 SDK 目标形态

```
ToySdk/                    ← 可独立分发的 SDK 目录
├── include/               ← 应用可见头（Unix CRT 名保持小写）
│   ├── toyos/
│   ├── stdio.h / stdlib.h / ...
│   ├── ToyUi.h / ToyGfx.h / ToyNet.h
│   └── ...
├── Library/               ← 预编译静态库（对齐 User/Library）
│   ├── libtoyos.a
│   ├── libToyUi.a
│   ├── libToyGfx.a
│   └── libToyNet.a
├── user.ld                ← 链接脚本
├── Examples/              ← 示例代码
├── Documents/             ← 开发指南
└── ToySdk.mk              ← 应用 Makefile 规则（对齐 ToyUser.mk）
```

**应用开发者只需拿到这个 SDK 目录 + 宿主机 gcc**，就能编译。当前过渡形态：直接用仓库 `User/`。

---

## 五、开发环境搭建

### 5.1 当前状态

应用开发者需要：

1. Linux 或 WSL2
2. gcc / ld / make；跑起来还要 QEMU（`qemu-system-x86_64`）
3. 克隆 ToyKernel，并与兄弟仓 **ToyImage**（QEMU 入口）放在一起
4. **过渡**：在 `User/Pkg/` 里写代码，用 `ToyUser.mk` 编译
5. **SDK（PR-A-sdk-pack ✅）**：`./Tools/build-sdk.sh` → `Dist/ToySdk/`，课外只带 SDK 目录
6. 复制 ELF 到 `ToyImage/rootfs/`
7. `./run-split.sh` 启动 QEMU

### 5.2 三种改进方向

| 方向 | 说明 | 落地难度 |
|------|------|----------|
| **A. 简化本地环境** | `Tools/build-sdk.sh` → `Dist/ToySdk/` | ✅ |
| **B. Docker 镜像** | `docker run toyos/dev` | 中（国内网络问题） |
| **C. Web IDE** | 浏览器里写代码 + 云端编译 | 高（1–2 月） |

**先做 A**，再做 C（B 因国内网络问题暂缓）。

### 5.3 方案 A 的目标

**一条命令产出 SDK**（已落地 `Tools/build-sdk.sh`）：

```bash
./Tools/build-sdk.sh
# 输出到 Dist/ToySdk/
```

**开发者使用**：

```bash
cd my-app/
make          # 编译出 MYAPP.ELF
make deploy   # 复制到 ToyImage/rootfs/
make run      # 启动 QEMU
```

---

## 六、文档盘点

### 6.1 现有文档

| 文档 | 内容 | 位置 |
|------|------|------|
| [`技术手册.md`](技术手册.md#tm-iv-libs) §IV | 开发库、GUI 入门、字体 | ✅ |
| [`User/Pkg/README.md`](../User/Pkg/README.md) | 应用模板用法 | ✅ |
| `User/include/*.h` | API 注释 | ⚠️ 部分 |
| **[`应用开发指南.md`](应用开发指南.md)** | 30 分钟上手 | ✅ **PR-A-doc-guide** |
| **API 参考手册** | ❌ | 无 |
| **示例程序索引** | ✅ [`Tools/Sdk/Examples/README.md`](../Tools/Sdk/Examples/README.md) | Hello File Dir Pipe Fork Gui Blit Net |
| **独立 FAQ** | △ 已收入指南第十三章 | 待拆成专页（P2） |

### 6.2 应该补的文档

| 文档 | 目标读者 | 长度 | 优先级 |
|------|----------|------|--------|
| **《ToyOS 应用开发指南》** | 应用开发者 | 5–10 页 | **P0 ✅** |
| **《ToyOS API 参考》** | 应用开发者 | 每 API 1–3 行 | P1 |
| **《ToyOS 示例程序集》** | 应用开发者 | 每示例 1 页 | P1 |
| **《ToyOS GUI 教程》** | GUI 应用开发者 | 5 页 | P1（现有技术手册 §IV GUI 入门） |
| **《ToyOS 网络教程》** | 网络应用开发者 | 3 页 | P2 |
| **《常见错误 FAQ》** | 应用开发者 | 2 页 | P2 |

---

## 七、PR 计划

### 7.1 PR 列表

| 序 | PR | 交付物 | 工作量 | 优先级 |
|----|----|--------|--------|--------|
| 1 | **PR-A-doc-guide** ✅ `c550f96` | [`应用开发指南.md`](应用开发指南.md)（≤10 页） | 2–3 天 | **P0** |
| 2 | **PR-A-sdk-pack** ✅ `c550f96` | `Tools/build-sdk.sh` + `Dist/ToySdk/` | 1–2 天 | **P0** |
| 3 | **PR-A-examples** ✅ `2c54664` | `Tools/Sdk/Examples/` 8 个示例 | 2–3 天 | **P0** |
| 4 | **PR-A-gfx-api** | libToyGfx 补全（点/线/矩形封装；blit 已有） | 3–5 天 | P1 |
| 5 | **PR-A-ui-api** | libToyUi 补全（输入框/列表/复选框） | 3–5 天 | P1 |
| 6 | **PR-A-net-dns** | libToyNet 加 sockaddr / 完整 DNS | 2–3 天 | P2 |
| 7 | **PR-A-fsutil** | libFsUtil（路径/目录/多卷） | 1–2 天 | P2 |

### 7.2 推荐顺序

```
PR-A-doc-guide（指南）✅
  → PR-A-sdk-pack（SDK 打包）✅
  → PR-A-examples（示例集）✅
  → PR-A-gfx-api（Gfx 补全）
  → PR-A-ui-api（Ui 补全）
  → PR-A-net-dns（网络扩展，可选）
  → PR-A-fsutil（FsUtil，可选）
```

**先有「指南 + SDK + 示例」三件套，再补库**——应用开发者最缺的是「怎么开始」，不是「更多 API」。

---

## 八、硬约束

1. **不改内核 API**：syscall 号、libToy 的 ABI 版本向后兼容
2. **不改现有应用**：`User/Pkg/` 下的模板保持可用
3. **静态库**：保持静态链接，不引入动态链接器
4. **不引入新依赖**：SDK 只依赖宿主机 gcc + make
5. **每 PR 独立可验证**：编译 + QEMU 跑通
6. **文档先行**：每个 PR 的文档优先于代码

---

## 九、相关文档

| 文档 | 作用 |
|------|------|
| [`应用开发指南.md`](应用开发指南.md) | 应用开发者上手（本文第一交付） |
| [`技术手册.md`](技术手册.md) | 分层、API、驱动、GUI 现状 |
| [`技术手册.md`](技术手册.md#tm-iv-gui) | GUI 应用教程 |
| [`路线图.md`](路线图.md) | 总体排期 |
| [`User/Pkg/README.md`](../User/Pkg/README.md) | 应用模板 |
| **本文** | 应用开发生态总纲 |

---

## 十、修订记录

| 日期 | 说明 |
|------|------|
| 2026-09-16 | 初版落地：现状盘点、缺口分析、PR 计划。按仓库现状校正 syscall 0–28、Gfx/Net ABI 1.1.0、`wait`/`OpenDirectory`/`ToyNetIpv4` 形态。**PR-A-doc-guide** ✅ |
| 2026-09-16 | 分 PR 进路线图 A 柱；JX **PR-A-sdk-pack** |
| 2026-09-16 | SDK 路径 PascalCase：`Tools/build-sdk.sh` → `Dist/ToySdk/`（`ToySdk.mk` / `Library` / `Examples`） |
| 2026-09-16 | **TG** A-doc-guide + A-sdk-pack `c550f96`；下一刀 **PR-A-examples** |
| 2026-09-16 | **PR-A-examples**：`Tools/Sdk/Examples/` Hello File Dir Pipe Fork Gui Blit Net |
| 2026-09-16 | **TG** A-examples `2c54664`；下一刀 **PR-A-gfx-api** |
