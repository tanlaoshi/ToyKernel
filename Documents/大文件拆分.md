# 任务：ToyKernel 大文件分 PR 拆分计划（>500 行）

> **规格性质同 [`XHCI拆分.md`](XHCI拆分.md)**：只搬家、不改逻辑；对外头文件与符号语义不变；每步可 build + smoke；真机相关步先验键鼠。  
> **统计时点**：2026-09-13 初版；**进度刷新** 同日（轨 A、B、C1–C3 已落地并 TG）。  
> **★ 功能/路线图当前 JX**：**PR-H-msc-7**（msc-6 `40e0a89`；见 [`路线图.md`](路线图.md)）。本文件是**拆分轨**；core-split ✅；**勿与 msc 同 PR 混改**。  
> **轨 A core 柱**：split-1…5 TG；**split-6** ✅ `dd61daf`（[`XHCI拆分2.md`](XHCI拆分2.md)）。

### ★ 拆分进度（2026-09-13）

| 状态 | PR | 提交（ToyKernel） | 说明 |
| --- | --- | --- | --- |
| ✅ | **PR-H-xhci-msc-split-1** | `a7a3f5b` | `XhciMsc.c`（函数）；MSC 全局留 Core；WaitCommand 独占事件环；NUC claim/capacity 过 |
| ✅ | **PR-H-xhci-msc-split-2** | `e41ac08` | `XhciEvent.c`（`ProcessEvents*`）；BSS 未迁 |
| ✅ | **PR-S-shell-split-1** | `320b9a8` | `ShellCmdUsb.c` + `ShellPriv.h` |
| ✅ | **PR-S-shell-split-2** | `31cb4d9` | `ShellCmdNet.c`（含 `ShellOnInterrupt`） |
| ✅ | **PR-S-shell-split-3** | `35c8766` | `ShellCmdSystem.c` + `ShellCmdFsUi.c`；`ShellCommands.c` 仅 Register |
| ✅ | **PR-S-sched-split-1** | `c1fd492` | `SchedRunq.c` + `SchedulerPriv.h`；runq/steal/PickNext |
| ✅ | **PR-S-console-split-1** | `c28b903` | `ConsoleScroll.c` + `ConsolePriv.h`；滚动缓冲 / 滚轮 |
| ✅ | **PR-S-sched-split-2** | `c35d144` | `SchedWait.c`；wait/zombie/exit |
| ✅ | **PR-S-syscall-split-1** | `257954b` | `SyscallFs.c` + `SyscallProc.c` + `SyscallPriv.h` |
| ✅ | **PR-H-xhci-core-split-1** | `40f88a8` | `XhciMmio.c` |
| ✅ | **PR-H-xhci-core-split-2** | `4f4df06` | `XhciRing.c` |
| ✅ | **PR-H-xhci-core-split-3** | `f771367` | `XhciCommand.c` |
| ✅ | **PR-H-xhci-core-split-4** | `5ab4c18` | `XhciTransfer.c`；Core ~1083 |
| ✅ | **PR-H-xhci-core-split-5** | `8456d0a` | `XhciController.c`；Core ~795 |
| ✅ | **PR-H-xhci-core-split-6** | `dd61daf` | `XhciKeyboard.c` + SetupHid→Device；Core ~679 |
| ⬜ | 轨 D… | — | FilesUi 等 |

**当前行数（约）**：`Syscall.c` ~149；`SyscallFs.c` ~287；`SyscallProc.c` ~86；`Scheduler.c` ~967；`Console.c` ~1295；`XhciCore.c` ~679；`XhciKeyboard.c` ~48；`XhciController.c` ~294；`XhciDevice.c` ~542；`XhciTransfer.c` ~64。

---

## 一、背景

| 教训 | 说明 |
| --- | --- |
| `XHCI.c` 拆前 | 约 **5000+** 行，已按 [`XHCI拆分.md`](XHCI拆分.md) 拆成 `Drivers/XHCI/*.c`（split-1…8 ✅） |
| 拆后仍超标 | **`XhciCore.c` 曾约 2626 行**；A1/A2 后约 **1577**（Init/Command 仍在 Core；MSC 逻辑→`XhciMsc`；事件→`XhciEvent`） |
| BSS 勿乱迁 | MSC/Bulk **全局**勿跨 `.c` 搬（会漂 HID DMA 环地址 → EnableSlot/鼠假死）；**只搬函数** |
| 其它热点 | Console / Scheduler / FilesUi / Video / Desktop / Net / GuiWm 仍 **>1100**；`ShellCommands.c` 已降至 ~815 |

目标：按功能切文件，单文件建议 **≤600～800 行**（与 XHCI 拆分建议一致）；允许个别编排入口略超。

### 1.1 明确不拆 / 后置

| 文件 | 行数 | 原因 |
| --- | ---: | --- |
| `Common/Library/DesktopAssetsData.c` | ~4472 | **生成/嵌入资源**，不是逻辑；拆分无意义 |
| `ThirdParty/lwip/**` | — | 第三方，不进本计划 |
| `tools/**` | — | 工具链解压树，不进本计划 |

---

## 二、清单（自有源码 >500 行）

按行数降序（约值；**已拆项用现况**，其余仍为 2026-09-13 初测）：

| 行数 | 路径 | 建议轨 | 优先级 / 状态 |
| ---: | --- | --- | --- |
| 679 | `HAL/X64/Drivers/XHCI/XhciCore.c` | A · XHCI-Core | A1–A8 ✅；柱后 msc-6 |
| 1295 | `Common/Services/Console.c` | B · Shell/Console | B4 ✅；仍可再瘦 |
| 967 | `Common/Core/Scheduler.c` | C · Scheduler | C1/C2 ✅ |
| 1411 | `Common/Services/FilesUi.c` | D · FilesUi | P2 |
| 1283 | `HAL/X64/Drivers/Video.c` | E · Video | P2 |
| 1242 | `Common/Services/Desktop.c` | F · Desktop/Gui | P2 |
| 1241 | `HAL/X64/Drivers/Net.c` | G · Net | P2 |
| 1192 | `Common/Services/GuiWm.c` | F · Desktop/Gui | P2 |
| 1059 | `Common/Services/Store.c` | H · Store | P3 |
| 1056 | `HAL/X64/AcpiMadt.c` | I · ACPI/SMP | P3 |
| 1035 | `Common/Library/FatDir.c` | J · Fat | P3 |
| 984 | `Common/Services/GuiCompose.c` | F · Desktop/Gui | P3 |
| 845 | `HAL/X64/Drivers/XHCI/XhciMsc.c` | A | ✅ split-1 |
| 848 | `HAL/X64/Drivers/XHCI/XhciHid.c` | （已拆；暂不动） | — |
| 815 | `Common/Services/ShellCommands.c` | B | B1–B3 ✅ → ~17 行 Register |
| 763 | `HAL/Virt/VirtioNet.c` | G · Net（virt） | P3 |
| 758 | `Common/Services/Theme.c` | F · Desktop/Gui | P3 |
| 708 | `HAL/X64/Drivers/XHCI/XhciHub.c` | （已拆；暂不动） | — |
| 693 | `Common/Core/Process.c` | C · Scheduler 旁 | P3 |
| 687 | `Common/Services/SettingsUi.c` | F · Desktop/Gui | P3 |
| 678 | `Common/Services/GuiDrag.c` | F · Desktop/Gui | P3 |
| 659 | `Common/Services/Tcp.c` | G · Net | P3 |
| 634 | `Common/Services/FileSystem.c` | J · Fat/FS | P3 |
| 621 | `Common/Library/Elf.c` | K · Elf | P4 |
| 607 | `Common/Services/StoreNet.c` | H · Store | P4 |
| 572 | `HAL/X64/HalSerial.c` | L · Serial | P4 |
| 567 | `Common/Core/TaskFd.c` | C · Scheduler 旁 | P4 |
| 566 | `HAL/X64/Drivers/XHCI/XhciMouse.c` | （已拆；暂不动） | — |
| 551 | `HAL/X64/LwIp/toy_socket.c` | G · Net | P4 |
| 528 | `Common/Services/ShellCmdNet.c` | B | ✅ split-2 |
| 522 | `HAL/X64/Drivers/Nvme.c` | M · Block | P4 |
| 149 | `Common/Core/Syscall.c` | C · Syscall | C3 ✅；仅 Dispatch |

**已拆 XHCI 对照**：`XhciDevice` / `XhciIrq` / `XhciPort` / `XhciDiag` / `XhciEvent` / `XhciMsc` / `XhciInternal.h`（共 10 个 `.c`）。

---

## 三、硬约束（全轨通用）

与 [`XHCI拆分.md`](XHCI拆分.md) 对齐：

1. **不改对外 API**：`Include/*.h`、`HAL/*/Drivers/*.h` 对 Common 可见的符号语义不变（可新增内部头）。  
2. **不改逻辑 / 宏值 / 结构体布局**；禁止「拆文件顺便修 bug / 改算法」。  
3. **每 PR 只搬一块**；`./build.sh` + `ToyImage/smoke-boot.sh`（或 virt smoke）通过。  
4. **真机路径**（XHCI / Video Present / Console 帧缓冲）：合并前 NUC 点验键鼠不回归。  
5. **勿与 msc-6… 混 PR**；MSC 功能刀与搬家刀分开。  
6. **全局变量**：定义落在「最相关」的 `.c`；其它文件经内部头 `extern`。**xHCI DMA 环/MSC 状态全局勿为瘦文件而跨 TU 迁**（BSS 序敏感）。  
7. 原 `static` 跨文件 → 去掉 static 并在内部头声明（命名可加前缀避免污染）。  
8. **WaitCommand 期间**：`XhciIrq` / `XhciDrainEvents` 勿并发 `ProcessEvents`（CCS 竞态）。

---

## 四、轨 A — XHCI Core 再拆（P0 · A1/A2 ✅；A3… 见 XHCI拆分2）

### 4.1 动机

`XhciCore.c` 曾混有：MMIO/环/命令、事件处理、控制器 Init、**MSC BringUp/claim/capacity/BOT**。A1/A2 后事件与 MSC **逻辑**已迁出；Init/Command/MMIO 仍在 Core（~1577 行）。

### 4.2 目标树（现况 + 第二轮）

```
HAL/X64/Drivers/XHCI/
├── XhciInternal.h
├── XhciCore.c          # 全局 + Init + Ready/Abandon（第二轮后）
├── XhciMmio.c …        # NEW：见 XHCI拆分2 / 路线图 ★
├── XhciMsc.c           # ✅ BringUp / Scan / Claim / Bulk / Capacity
├── XhciEvent.c         # ✅ ProcessEvents / ProcessEventsRealPc
└── …
```

对外仍 `HAL/X64/Drivers/XHCI.h`。规格：**[`XHCI拆分2.md`](XHCI拆分2.md)**（插入 msc-6 之前）。

### 4.3 分 PR

| 序 | PR | 内容 | 故意不做 | 状态 |
| -- | -- | ---- | -------- | --- |
| 1 | **PR-H-xhci-msc-split-1** | `XhciMsc.c`：只搬 MSC **函数**；全局留 Core | 不改 claim；不挂 FAT；不搬全局 | ✅ `a7a3f5b`；NUC 过 |
| 2 | **PR-H-xhci-msc-split-2** | `XhciEvent.c`：`ProcessEvents*` | 不改 Drain/IRQ；不搬全局 | ✅ `e41ac08` |
| 3 | **PR-H-xhci-core-split-1** | `XhciMmio.c` | 不搬 BSS | ✅ `40f88a8` |
| 4 | **PR-H-xhci-core-split-2** | `XhciRing.c` | 不搬 BSS | ✅ `4f4df06` |
| 5 | **PR-H-xhci-core-split-3** | `XhciCommand.c` | 不搬 BSS | ✅ `f771367` |
| 6 | **PR-H-xhci-core-split-4** | `XhciTransfer.c` | 不搬 BSS | ✅ `5ab4c18` |
| 7 | **PR-H-xhci-core-split-5** | `XhciController.c` | 不搬 BSS；不抽 Init | ✅ `8456d0a` |
| 8 | **PR-H-xhci-core-split-6** | Keyboard + SetupHid→Device | 不搬 BSS | ✅ `dd61daf` |

### 4.4 Makefile

`wildcard HAL/.../Drivers/XHCI/*.c`（与 split-8 同）；新 `.c` 自动编入。

---

## 五、轨 B — Console + ShellCommands（P1 · B1–B4 ✅）

### 5.1 现状

| 文件 | 职责 / 现况 |
| --- | --- |
| `Console.c` | ~1295：输出、提示符、注册表、help、输入分发；绘制入口仍在此 |
| `ConsoleScroll.c` | ✅ 行缓冲 / 历史 / Paint / `ConsoleOnWheel` |
| `ConsolePriv.h` | ✅ `SB_*`、draw helpers、`ConsoleSb*` |
| `ShellCommands.c` | ~17：仅 `ShellCommandsRegister` / `VirtMin` 汇总 |
| `ShellCmdUsb.c` | ✅ xhci / msc |
| `ShellCmdNet.c` | ✅ net/ping/dns/udp/tcp/lwip + `ShellOnInterrupt` |
| `ShellCmdSystem.c` | ✅ info/mem/ps/kill/reboot/exec/halt/lsdev… |
| `ShellCmdFsUi.c` | ✅ shell/settings/files/edit/font/store/lang/zh |
| `ShellCommandsFs.c` | 已有：ls/cat/vols…（`RegisterFs`） |

### 5.2 目标树

```
Common/Services/
├── Console.c              # 输出、提示符、注册表、分发循环
├── ConsoleScroll.c        # ✅ 行缓冲 / 历史 / Paint / OnWheel
├── ShellCommands.c        # ✅ Register 汇总
├── ShellCmdSystem.c       # ✅
├── ShellCmdNet.c          # ✅
├── ShellCmdFsUi.c         # ✅
└── ShellCmdUsb.c          # ✅
```

内部头：`Include/ShellPriv.h`、`Include/ConsolePriv.h`（User 勿 include）。

### 5.3 分 PR

| 序 | PR | 内容 | 验收 | 状态 |
| -- | -- | ---- | ---- | --- |
| 1 | **PR-S-shell-split-1** | `ShellCmdUsb.c` | build + help / msc / xhci | ✅ `320b9a8` |
| 2 | **PR-S-shell-split-2** | `ShellCmdNet.c` | ping 冒烟 | ✅ `31cb4d9` |
| 3 | **PR-S-shell-split-3** | `ShellCmdSystem.c` + `ShellCmdFsUi.c`；Register 汇总 | help 列表完整 | ✅ `35c8766` |
| 4 | **PR-S-console-split-1** | 滚动缓冲 → `ConsoleScroll.c` + `ConsolePriv.h` | build + smoke | ✅ `c28b903` |
## 六、轨 C — Scheduler / Process / Syscall（P1）

### 6.1 现状

`Scheduler.c`：runq、创建任务、调度、僵尸回收、信号默认终止、与 TaskFd 边界（C1/C2 ✅）。  
`Syscall.c`：仅 `SyscallDispatch`（C3 ✅）；实现见 `SyscallFs` / `SyscallProc`。

### 6.2 目标树

```
Common/Core/
├── Scheduler.c          # Init / Create / Activate / fork/kill；主循环
├── SchedRunq.c          # ✅ 每核 runq / steal / affinity
├── SchedWait.c          # ✅ wait/zombie/reap/exit
├── Process.c            # 已有；保持
├── TaskFd.c             # 已有；保持
├── Syscall.c            # ✅ 仅 Dispatch + Init
├── SyscallFs.c          # ✅ open/read/write/close/dir/pipe/dup/socket*
└── SyscallProc.c        # ✅ execve + 用户窗 GUI
```

内部头：`Include/SchedulerPriv.h`、`Include/SyscallPriv.h`（仅 Core 用）。

### 6.3 分 PR

| 序 | PR | 内容 | 验收 | 状态 |
| -- | -- | ---- | ---- | --- |
| 1 | **PR-S-sched-split-1** | `SchedRunq.c`（runq/steal/PickHome/PickNext）+ `SchedulerPriv.h` | smoke；`ps`；`TOY_SMP=2` | ✅ `c1fd492` |
| 2 | **PR-S-sched-split-2** | `SchedWait.c`（wait/zombie/reap/exit）+ Priv 共享 Terminate | fork/wait/kill 冒烟 | ✅ `c35d144` |
| 3 | **PR-S-syscall-split-1** | `SyscallFs.c` + `SyscallProc.c` + `SyscallPriv.h` | build + smoke；`exec HELLO.ELF` | ✅ `257954b` |

---

## 七、轨 D — FilesUi（P2）

### 7.1 目标树

```
Common/Services/
├── FilesUi.c            # Open/事件入口、状态机
├── FilesUiPaint.c       # NEW：PaintList/View/Prompt/Confirm
├── FilesUiNav.c         # NEW：路径、书签、ReloadList
└── FilesUiActions.c     # NEW：Open/Delete/PromptCommit
```

内部头：`FilesUiPriv.h`。

### 7.2 分 PR

| 序 | PR | 内容 | 验收 |
| -- | -- | ---- | ---- |
| 1 | **PR-U-files-split-1** | Paint* → `FilesUiPaint.c` | Files 窗列表/预览 |
| 2 | **PR-U-files-split-2** | Nav + Actions | 打开/删除/新建 |

---

## 八、轨 E — Video（P2）

### 8.1 目标树

```
HAL/X64/Drivers/
├── Video.c              # Set/Present 入口、缩放、clip API
├── VideoPresent.c       # NEW：脏区 / 条带 Present / 光标脏
├── VideoDraw.c          # NEW：DrawString/Fill/Copy/Scroll
└── VideoBochs.c         # NEW：DISPI 热切（可选）
```

对外：`Video.h` / `HalVideo.h` 不变。

### 8.2 分 PR

| 序 | PR | 内容 | 验收 |
| -- | -- | ---- | ---- |
| 1 | **PR-G-video-split-1** | Present 路径单独文件 | smoke；真机拖窗/打字（Present 饿死 HID 回归） |
| 2 | **PR-G-video-split-2** | Draw* + Bochs | QEMU 热切若有 |

---

## 九、轨 F — Desktop / GuiWm / Compose（P2～P3）

### 9.1 目标树

```
Common/Services/
├── Desktop.c            # 入口、FillRect、图标布局
├── DesktopIcons.c       # NEW：BMP 加载/blit/图标绘制
├── DesktopTaskbar.c     # NEW：任务栏/开始菜单/时钟
├── GuiWm.c              # 窗口表、Raise、点击命中
├── GuiWmOpen.c          # NEW：OpenShell/Settings/Files/Store/Edit
├── GuiCompose.c         # 已有；保持或再切脏区
├── GuiDrag.c            # 已有
└── SettingsUi.c / Theme.c  # P3 再议
```

### 9.2 分 PR

| 序 | PR | 内容 | 验收 |
| -- | -- | ---- | ---- |
| 1 | **PR-G-desk-split-1** | Icons + Taskbar 抽出 | 桌面图标/开始菜单 |
| 2 | **PR-G-wm-split-1** | `GuiWmOpen.c` | 开窗不回归 |

---

## 十、轨 G — Net（P2～P3）

### 10.1 目标树

```
HAL/X64/Drivers/
├── Net.c                # 对外 Hal 门面胶水（可极瘦）
├── VirtioNetPci.c       # NEW：从 Net.c 搬 virtqueue + virtio-net
├── NetArpIcmp.c         # NEW：ARP/ICMP/IPv4 收发
HAL/Virt/VirtioNet.c     # Arm/RiscV；可对称命名，P3
Common/Services/Tcp.c    # 保持或拆 TcpConn.c
```

### 10.2 分 PR

| 序 | PR | 内容 | 验收 |
| -- | -- | ---- | ---- |
| 1 | **PR-N-net-split-1** | Virtio PCI + queue → 独立文件 | QEMU ping |
| 2 | **PR-N-net-split-2** | ARP/ICMP | 同上 |

---

## 十一、轨 H～M（P3～P4 · 摘要）

| 轨 | 文件 | 建议切法 | 优先级 |
| --- | --- | --- | --- |
| H Store | `Store.c` / `StoreNet.c` | Catalog / Install / Net fetch | P3 |
| I ACPI | `AcpiMadt.c` | MADT 解析 vs IOAPIC/电源表 | P3 |
| J Fat | `FatDir.c` / `FileSystem.c` | 目录遍历 vs 卷挂载 | P3 |
| K Elf | `Elf.c` | 加载 vs reloc | P4 |
| L Serial | `HalSerial.c` | GOP boot log vs PHOTO（已偏清晰，可后置） | P4 |
| M Nvme | `Nvme.c` | Admin vs I/O 队列 | P4 |

---

## 十二、推荐落地顺序（与路线图关系）

```
（功能）PR-H-msc-6 ✅ `40e0a89`；msc-7 ← ★；与拆分错开
        │
A1 ✅  xhci-msc-split-1
A2 ✅  xhci-msc-split-2（XhciEvent）
A3 ✅  xhci-core-split-1（Mmio；`40f88a8`）
A4 ✅  xhci-core-split-2（Ring；`4f4df06`）
A5 ✅  xhci-core-split-3（Command；`f771367`）
A6 ✅  xhci-core-split-4（Transfer；`5ab4c18`）
A7 ✅  xhci-core-split-5（Controller；`8456d0a`）
A8 ✅  xhci-core-split-6（Keyboard+SetupHid；`dd61daf`）
B1 ✅  shell-split-1（Usb）
B2 ✅  shell-split-2（Net）
B3 ✅  shell-split-3（System+FsUi）
B4 ✅  console-split-1（ConsoleScroll）
C1 ✅  sched-split-1（SchedRunq）
C2 ✅  sched-split-2（SchedWait）
C3 ✅  syscall-split-1（SyscallFs/Proc）
        │
D1 / E1 / F1 / G1 …
```

**原则**：功能刀（msc）与搬家刀错开；同一周不要既大改 claim 又搬 Core。

---

## 十三、验收清单（每个拆分 PR）

- [ ] `./build.sh`（x64）通过  
- [ ] `ToyImage/smoke-boot.sh` 通过  
- [ ] 对外头文件无破坏性变更（diff 仅新增内部头 / Makefile）  
- [ ] 无行为改动意图（code review：纯 move）  
- [ ] 若动 XHCI / Video Present / Console GOP：真机键鼠点验  
- [ ] 路线图：完成后从前半删除，**追加文末【归档】**（同 XHCI split）  
- [ ] 本文件：进度表 / 分 PR 表标 ✅，修订记录追加一行  

---

## 十四、文档与 Makefile 惯例

- 每轨可在本文件下追加「落地记录」小节，或另建 `Documents/XXX拆分.md`（如 `XhciMsc拆分.md`）写函数级清单。  
- Makefile：XHCI / Services 均 `wildcard *.c`，新文件自动编入。  
- 内部头命名：`*Priv.h` / `*Internal.h`；**禁止** User 程序 include。

---

## 十五、修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-09-13 | 初版：基于 >500 行清单；对标 `XHCI拆分.md`；P0=XhciMsc 从 Core 抽出 |
| 2026-09-13 | split-1 落地（函数→XhciMsc；全局留 Core）+ WaitCommand 独占事件环；NUC claim/capacity 过 |
| 2026-09-13 | split-2：`ProcessEvents*` → `XhciEvent.c`（仍不迁 BSS） |
| 2026-09-13 | **PR-S-shell-split-1**：`ShellCmdUsb.c` + `ShellPriv.h`（xhci/msc 命令） |
| 2026-09-13 | **PR-S-shell-split-2**：`ShellCmdNet.c`（net/ping/dns/udp/tcp/lwip + ShellOnInterrupt） |
| 2026-09-13 | 文档：文首 ★ 进度表；A1/A2、B1/B2 标 ✅；刷新行数与推荐顺序（下一刀 B3） |
| 2026-09-13 | **PR-S-shell-split-3**：`ShellCmdSystem.c` + `ShellCmdFsUi.c`；`ShellCommands.c` 仅 Register；下一刀 C1 |
| 2026-09-13 | **PR-S-sched-split-1**：`c1fd492` — `SchedRunq.c` + `SchedulerPriv.h`；smoke `TOY_SMP=2` 过 |
| 2026-09-13 | **PR-S-console-split-1**：`c28b903` — `ConsoleScroll.c` + `ConsolePriv.h`；`Console.c` ~1295；build+smoke 过 |
| 2026-09-13 | TG：sched-split-1 + console-split-1 → ToyImage Kernel sync |
| 2026-09-13 | **PR-S-sched-split-2**：`c35d144` — `SchedWait.c`；`Scheduler.c` ~967；smoke + TOY_SMP=2 过 |
| 2026-09-13 | TG：sched-split-2 → ToyImage Kernel sync |
| 2026-09-13 | **PR-S-syscall-split-1**：`257954b` — `SyscallFs.c` + `SyscallProc.c` + `SyscallPriv.h`；`Syscall.c` ~149；build+smoke 过 |
| 2026-09-13 | TG：syscall-split-1 → ToyImage Kernel sync |
| 2026-09-13 | **XHCI拆分2**：6 刀 core-split；禁 BSS 搬；插入路线图 ★（msc-6 之前） |
| 2026-09-13 | **PR-H-xhci-core-split-1**：`40f88a8` — `XhciMmio.c`；Core ~1410；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-1 `40f88a8` → ToyImage Kernel sync |
| 2026-09-13 | **PR-H-xhci-core-split-2**：`4f4df06` — `XhciRing.c`；Core ~1302；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-2 `4f4df06` → ToyImage Kernel sync |
| 2026-09-13 | **PR-H-xhci-core-split-3**：`f771367` — `XhciCommand.c`；Core ~1146；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-3 `f771367` → ToyImage Kernel sync |
| 2026-09-13 | **PR-H-xhci-core-split-4**：`5ab4c18` — `XhciTransfer.c`；Core ~1083；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-4 `5ab4c18` → ToyImage Kernel sync |
| 2026-09-13 | **PR-H-xhci-core-split-5**：`8456d0a` — `XhciController.c`；Core ~795；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-5 `8456d0a` → ToyImage Kernel sync |
| 2026-09-13 | **PR-H-xhci-core-split-6**：`dd61daf` — `XhciKeyboard.c` + SetupHid→Device；Core ~679；build+smoke 过 |
| 2026-09-13 | TG：xhci-core-split-6 `dd61daf` → ToyImage；★ → msc-6 |
| 2026-09-13 | TG：msc-6 `40e0a89` → ToyImage Kernel sync |
