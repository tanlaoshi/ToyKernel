# ToyOS 开发命名规范

> **本文是 ToyOS 所有代码的命名约束。** 新增代码必须遵守；旧代码逐步迁移。
>
> 相关文档：
> - [`技术手册.md`](技术手册.md) — 分层与接口（§I）；库与 GUI 见 §IV
> - [`路线图.md`](路线图.md#五c-标识符命名规划) §五 — C 标识符命名规划（历史迁移清单）

---

## 一、总则

### 1.1 核心原则

| 原则 | 说明 |
|------|------|
| **PascalCase** | 每个单词首字母大写，单词之间无下划线 |
| **全词拼写** | 不缩写、不省略（`Configure` 不是 `Cfg`） |
| **类型全大写** | 结构体、枚举、宏用 `ALL_CAPS` |
| **全局 `g` 前缀** | 全局变量用 `g` + PascalCase |
| **静态 `s` 前缀** | 文件内静态变量用 `s` + PascalCase（可选） |
| **分层前缀** | 按层加前缀（`Hal*` / `Toy*` / `Kernel*`） |

### 1.2 为什么这样

| 理由 | 说明 |
|------|------|
| **可读性** | PascalCase 在英文中更接近自然语言 |
| **搜索友好** | 全词拼写让 `grep Configure` 能找到所有相关代码 |
| **与 EDK2 一致** | UEFI 参考实现用 PascalCase，ToyOS 也走这条路 |
| **避免缩写歧义** | `Cfg` 可能是 Configure / Config / Configuration，全词避免歧义 |

### 1.3 现状（2026-09 抽样）

内核公开头（`Include/*.h`）与 `Common/Core/`、`HAL/X64/`（不含 LwIP）的函数几乎全部是 **PascalCase**。抽样未见内核 API 用 snake_case / camelCase。

`HAL/X64/LwIp/` 是第三方：允许 `sys_now` 等原项目风格，不按本规范改。

**明确例外（保持，不迁成 PascalCase）**：

- POSIX / CRT：`User/include/*.h`、`User/crt/` 的 `fopen` / `malloc` / `lseek` / `stdio.h` / `user.ld` / `crt0.S`
- 脚本：`build.sh`、`Tools/build-sdk.sh`（kebab-case）
- FAT 盘上产品名：`MYAPP.ELF` 等 8.3 大写

---

## 二、函数命名

### 2.1 基本规则

```
动词 + 名词（PascalCase）
```

**示例**：
| 正确 | 错误 |
|------|------|
| `InitializeSerial` | `InitSerial` / `init_serial` |
| `ConfigureInterrupt` | `CfgIrq` / `config_int` |
| `HalSerialWrite` | `hal_serial_write` |
| `ToyDriverRegister` | `toy_drv_reg` |
| `VirtualMemoryMapPage` | `vmm_map` |
| `FileSystemReadFile` | `fs_read` |

### 2.2 分层前缀

| 层 | 前缀 | 示例 |
|----|------|------|
| **HAL** | `Hal*` | `HalSerialWrite` / `HalVideoFillRect` |
| **驱动框架** | `ToyDriver*` | `ToyDriverRegister` / `ToyDriverProbeAll` |
| **Core** | 无前缀或 `Kernel*` | `KernelMain` / `SchedulerCreate` |
| **Library** | 无前缀 | `FatReadFile` / `GptFindFatStart` |
| **Services** | 无前缀 | `ConsoleWrite` / `GuiOpenShell` |
| **用户态库** | `Toy*` | `ToyUiCreateWindow` / `ToyGfxDamageRect` |
| **Board** | `Board*` | `BoardInit` / `BoardName` |

### 2.3 常用动词

| 动词 | 用途 | 示例 |
|------|------|------|
| `Initialize` | 初始化 | `InitializeSerial` |
| `Configure` | 配置 | `ConfigureInterrupt` |
| `Register` | 注册 | `ToyDriverRegister` |
| `Attach` | 挂载 | `ToyDriverInputAttach` |
| `Probe` | 探测 | `XhciDriverProbe` |
| `Bind` | 绑定 | `XhciDriverBind` |
| `Remove` | 移除 | `XhciDriverRemove` |
| `Read` / `Write` | 读写 | `FatReadFile` / `HalSerialWrite` |
| `Get` / `Set` | 存取 | `HalGetCpuId` / `ThemeSetFontId` |
| `Allocate` / `Free` | 分配/释放 | `PhysicalMemoryAllocatePages` |
| `Create` / `Destroy` | 创建/销毁 | `SchedulerCreate` / `WindowDestroy` |
| `Open` / `Close` | 打开/关闭 | `FileSystemOpen` / `ConsoleClose` |
| `Enable` / `Disable` | 启用/禁用 | `HalIrqEnable` / `HalIrqDisable` |
| `Start` / `Stop` | 启动/停止 | `SchedulerStart` / `TimerStop` |
| `Load` / `Store` | 加载/存储 | `ElfLoadFromMemory` / `DbSet` |
| `Parse` / `Format` | 解析/格式化 | `FatParsePath` / `HalSerialFormatHex` |
| `Handle` | 处理 | `InterruptHandle` |
| `Dispatch` | 分发 | `SyscallDispatch` |
| `Poll` | 轮询 | `XhciInputPoll` |
| `Flush` | 刷新 | `BlockFlush` |

历史模块入口仍大量使用后缀 `Init`（`FileSystemInit` / `HalSerialInit` / `VirtualMemoryInit`）。**新增**入口用 `Initialize*`；旧 `*Init` 按 [`路线图.md`](路线图.md#五c-标识符命名规划) §五第二轮再统一。

### 2.4 返回类型与命名约定

| 返回类型 | 命名提示 | 示例 |
|----------|----------|------|
| `int`（成功 0，失败负） | 无特殊 | `FatReadFile` |
| `int`（成功 1，失败 0） | 加 `Is` / `Has` / `Can` | `IsDirectory` / `HasEvent` |
| `void` | 加动作动词 | `ConsoleWrite` |
| 指针 | 加 `Get` / `Create` / `Allocate` | `GetCurrent` / `CreateWindow` |
| 布尔 | 加 `Is` / `Has` / `Should` | `IsReady` / `HasData` |

### 2.5 POSIX / CRT 例外

用户态 Unix 兼容层**保持小写 + 下划线**，与主机 libc 对齐，**不**改成 PascalCase：

```c
int open(const char *path, int flags);
FILE *fopen(const char *path, const char *mode);
void *malloc(size_t n);
off_t lseek(int fd, off_t offset, int whence);
```

ToyOS 自有库仍用分层前缀：`ToyUiCreateWindow`、`ToyGfxDamageRect`、`ToyNetResolve`。

---

## 三、变量命名

### 3.1 局部变量

```
PascalCase（循环短名除外）
```

**推荐**：局部变量与函数参数用 **PascalCase**：

```c
void ProcessData(int BufferSize, char *InputBuffer) {
    int Index;
    int Count;
    char *Pointer;

    for (Index = 0; Index < BufferSize; Index++) {
        Count++;
    }
}
```

**允许的简写**：仅少数约定俗成的短名：

- `i` / `j` / `k`（循环索引，仅限简单循环）
- `x` / `y` / `z`（坐标，仅限几何）
- `fd`（文件描述符）
- `id`（标识符）
- `pid`（进程 ID）

### 3.2 全局变量

```
g + PascalCase
```

**示例**：
| 正确 | 错误 |
|------|------|
| `gCommands` | `commands` / `g_commands` |
| `gDriverCount` | `gDrvCnt` |
| `gSchedulerLock` | `gSchedLock` |
| `gActiveWindow` | `gActiveWin` |

现码仍有 `gCmdCount`、`gSaveW` 这类半缩写，迁移时写全（`gCommandCount`、`gSaveWidth`）。

### 3.3 静态变量

```
s + PascalCase（可选）
```

**示例**：
```c
static int sInitialized;
static UINT32 sBuffer[1024];
```

**注**：也可以用无前缀的 PascalCase。文件内 `static` 优先 `s`；跨文件可见用 `g`（现码里不少 `static` 仍用 `g`，如 `gRunq`，不强制一次改完）。

### 3.4 指针变量

```
PascalCase，无需特殊前缀
```

**示例**：
```c
char *Buffer;
TOY_DRIVER *Driver;
HAL_INTERRUPT_FRAME *Frame;
```

**不要用**匈牙利命名法（`pBuffer` / `lpDriver`）。

### 3.5 常量

```
ALL_CAPS（宏）或 const PascalCase
```

**宏**（推荐）：
```c
#define MAX_TASKS 8
#define FAT_OK 0
#define PORTSC_CCS (1u << 0)
```

**const 变量**（可选）：
```c
static const UINT32 DefaultWindowWidth = 400;
static const char *const DefaultFontName = "Terminus16x32";
```

---

## 四、类型命名

### 4.1 结构体

```
ALL_CAPS（下划线分隔）
```

**示例**：
```c
typedef struct {
    ...
} TOY_DRIVER;

typedef struct {
    ...
} HAL_INTERRUPT_FRAME;

typedef struct {
    ...
} FAT_DIRECTORY_ENTRY;
```

**对比**：Linux 用 `struct toy_driver`，ToyOS 用 `TOY_DRIVER`。

用户态自有类型同样 ALL_CAPS（`TOY_GFX_DAMAGE_RECT`、`TOY_DIR`）。**不要**再新增 `ToySockAddrIn` 这种 PascalCase typedef；旧名按 §八 迁移。

### 4.2 枚举

```
ALL_CAPS 类型名 + ALL_CAPS 成员
```

**示例**：
```c
typedef enum {
    TOY_DRIVER_CLASS_NONE = 0,
    TOY_DRIVER_CLASS_BLOCK,
    TOY_DRIVER_CLASS_INPUT,
    TOY_DRIVER_CLASS_NET,
    TOY_DRIVER_CLASS_DISPLAY
} TOY_DRIVER_CLASS;

typedef enum {
    GUI_WIN_NONE = 0,
    GUI_WIN_SHELL,
    GUI_WIN_SETTINGS,
    GUI_WIN_FILES
} GUI_WIN_KIND;
```

### 4.3 typedef

```
ALL_CAPS（与结构体、枚举一致）
```

**示例**：
```c
typedef UINT64 PHYSICAL_ADDRESS;
typedef UINT32 PROCESS_ID;
```

POSIX 头例外：`FILE`、`size_t`、`off_t` 保持 Unix 惯用名。

---

## 五、宏命名

### 5.1 常量宏

```
ALL_CAPS（下划线分隔）
```

**示例**：
```c
#define MAX_TASKS 8
#define PAGE_SIZE 4096
#define FAT_ENT_NAME_MAX 64
#define PORTSC_CCS (1u << 0)
```

### 5.2 函数宏

```
ALL_CAPS 或 PascalCase（视复杂度）
```

**简单的函数宏**（推荐 ALL_CAPS）：
```c
#define ALIGN_UP(x, a) (((x) + (a) - 1) & ~((a) - 1))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
```

**复杂的函数宏**（推荐 PascalCase，模拟函数）：
```c
#define ListForEach(Entry, List) \
    for ((Entry) = (List)->Head; (Entry); (Entry) = (Entry)->Next)
```

**优先用函数**：如果可以用 static inline 函数代替，就不要用宏。

### 5.3 编译开关

```
TOY_* 前缀
```

**示例**：
```c
#define TOY_DEBUG 1
#define TOY_DEMO_DRIVER 1
#define TOY_USB_MSC 1
```

**编译命令**：
```bash
./build.sh TOY_DEBUG=1 TOY_DEMO_DRIVER=0
```

---

## 六、文件命名

### 6.1 源文件

```
PascalCase.c / PascalCase.h
```

**示例**：
| 正确 | 错误 |
|------|------|
| `VirtualMemory.c` | `virtual_memory.c` / `vmm.c` |
| `PhysicalMemory.c` | `pmm.c` |
| `FileSystem.c` | `filesystem.c` / `fs.c` |
| `Scheduler.c` | `sched.c` |
| `XhciCore.c` | `xhci_core.c` |

**例外**（保持现状）：

- `crt0.S` / `syscall.S`（CRT 惯例）
- `user.ld`（链接脚本惯例）
- `User/include/stdio.h` 等 POSIX 头（小写）
- `build.sh` / `Tools/build-sdk.sh`（脚本 kebab-case）

### 6.2 目录

```
PascalCase/
```

**示例**：
| 正确 | 错误 |
|------|------|
| `Common/Core/` | `common/core/` |
| `HAL/X64/` | `hal/x64/` |
| `User/Library/ToyUi/` | `user/library/toyui/` |
| `User/Pkg/` | `user/pkg/` |
| `Drivers/XHCI/` | `drivers/xhci/` |
| `Tools/Sdk/Examples/Hello/` | `tools/sdk/examples/hello/` |

**例外**：

- `HAL/`（全大写，与 EDK2 一致）
- `X64` / `Arm64` / `RiscV`（架构名；`RiscV` 目录保持现状，不改 `RiscV64`）
- `Virt/`（虚拟平台）

### 6.3 脚本与命令行产物

```
kebab-case（仅 *.sh 与仓库根构建入口）
```

**示例**：`build.sh`、`Tools/build-sdk.sh`、`Tools/check-naming.sh`（若将来增加）。

FAT 根上的 ELF **仍是 8.3 大写**（`HELLO.ELF`），与源码目录名无关。

---

## 七、缩写白名单

以下缩写**保留**，不强制写全：

| 缩写 | 全称 | 用途 |
|------|------|------|
| **CPU** | Central Processing Unit | 处理器 |
| **HAL** | Hardware Abstraction Layer | 硬件抽象层 |
| **IRQ** | Interrupt Request | 中断请求 |
| **IO** / **MMIO** | Input/Output / Memory-Mapped IO | I/O |
| **PCI** / **PCIe** | Peripheral Component Interconnect | PCI 总线 |
| **USB** | Universal Serial Bus | USB 总线 |
| **XHCI** | eXtensible Host Controller Interface | USB 3.x 控制器 |
| **AHCI** | Advanced Host Controller Interface | SATA 控制器 |
| **NVMe** | Non-Volatile Memory express | NVMe 硬盘 |
| **FAT** | File Allocation Table | FAT 文件系统 |
| **ELF** | Executable and Linkable Format | ELF 文件 |
| **GPT** | GUID Partition Table | GPT 分区 |
| **ACPI** | Advanced Configuration and Power Interface | ACPI |
| **MADT** | Multiple APIC Description Table | ACPI 子表 |
| **GOP** | Graphics Output Protocol | UEFI 显示协议 |
| **UEFI** | Unified Extensible Firmware Interface | UEFI |
| **BIOS** | Basic Input/Output System | BIOS |
| **DTB** | Device Tree Blob | 设备树 |
| **SMP** | Symmetric Multiprocessing | 对称多处理 |
| **APIC** | Advanced Programmable Interrupt Controller | APIC |
| **LAPIC** | Local APIC | 本地 APIC |
| **IOAPIC** | I/O APIC | I/O APIC |
| **GIC** | Generic Interrupt Controller | ARM 中断控制器 |
| **TSC** | Time Stamp Counter | x86 时间戳计数器 |
| **RTC** | Real-Time Clock | 实时时钟 |
| **CRT** | C Runtime | C 运行库 |
| **ABI** | Application Binary Interface | 应用二进制接口 |
| **API** | Application Programming Interface | 应用编程接口 |
| **GUI** | Graphical User Interface | 图形用户界面 |
| **HID** | Human Interface Device | USB HID 设备 |
| **MSC** | Mass Storage Class | USB 存储类 |
| **DMA** | Direct Memory Access | DMA |
| **COW** | Copy-On-Write | 写时复制 |
| **PID** | Process Identifier | 进程 ID |
| **FD** | File Descriptor | 文件描述符 |
| **PMM** | Physical Memory Manager | 物理内存管理（注释可保留；函数名仍用 `PhysicalMemory*`） |
| **VMM** | Virtual Memory Manager | 虚拟内存管理（注释可保留；函数名仍用 `VirtualMemory*`） |
| **TSS** | Task State Segment | x86 任务状态段 |
| **GDT** | Global Descriptor Table | 全局描述符表 |
| **IDT** | Interrupt Descriptor Table | 中断描述符表 |
| **ISR** | Interrupt Service Routine | 中断服务例程 |
| **TRB** | Transfer Request Block | xHCI 传输请求块 |
| **DCI** | Device Context Index | xHCI 设备上下文索引 |
| **LBA** | Logical Block Address | 逻辑块地址 |
| **MPS** | Max Packet Size | USB 最大包大小 |
| **BSP** | Bootstrap Processor | 启动处理器（见 §八：`HalCpuIsBsp` 可保留或写全） |
| **SDK** | Software Development Kit | 用户态 SDK |

**不在白名单的缩写**（应写全）：

| 缩写 | 应写成 |
|------|--------|
| `Init` | `Initialize`（新代码；旧 `*Init` 入口见 §2.3） |
| `Cfg` | `Configure` / `Configuration` |
| `Msg` | `Message` |
| `Buf` | `Buffer` |
| `Len` | `Length` |
| `Ptr` | `Pointer` |
| `Idx` | `Index` |
| `Cnt` | `Count` |
| `Tbl` | `Table` |
| `Ctx` | `Context` |
| `Param` | `Parameter` |
| `Attr` | `Attribute` |
| `Desc` | `Descriptor` |
| `Reg` | `Register` |
| `Addr` | `Address` |
| `Fn` | `Function` |
| `Drv` | `Driver` |
| `Sched` | `Scheduler` |
| `Fs` | `FileSystem` |
| `Mem` | `Memory` |
| `Kbd` | `Keyboard` |
| `Mou` | `Mouse` |
| `Win` | `Window` |
| `Wm` | `WindowManager` |
| `Cmd` | `Command` |
| `Runq` | `RunQueue` |

---

## 八、迁移策略

### 8.1 新代码

**必须**遵守本规范。

### 8.2 旧代码

**逐步迁移**，不强制一次改完。迁移原则：

1. **改代码时顺手改**：修改某个函数时，把该文件内的旧命名改为新命名
2. **不跨文件改名**：一次 PR 只改一个模块的命名
3. **改名前先冻结接口**：如果符号是对外 API，先讨论再改
4. **每次迁移独立 PR**：便于 review 和回滚

### 8.3 已知的历史遗留

对照源码与 [`路线图.md`](路线图.md#pr-r-name) **R 柱**（第二轮默认排队）及 [`路线图.md`](路线图.md#五c-标识符命名规划) §五：

| 位置 | 旧名 | 新名 | 排期 |
|------|------|------|------|
| `HalCpuId` | `HalGetCpuId` | ✅ 已改（R6） | — |
| `HalSmpStartAps` | `HalSmpStartApplicationProcessors` | ✅ 已改（R6） | — |
| `HalPageIsCow` | `HalPageIsCopyOnWrite` | ✅ 已改（R6） | — |
| `ThemeDesktopBg` | `ThemeDesktopBackground` | ✅ 已改（R7） | — |
| `GuiSetFocusWin` | `GuiSetFocusWindow` | ✅ 已改（R7） | — |
| `VM_ADDR_SPACE` | `VIRTUAL_ADDRESS_SPACE` | ✅ 已改（R9） | — |
| `FAT_DIR_ENT` | `FAT_DIRECTORY_ENTRY` | ✅ 已改（R9） | — |
| `CPU_RUNQ` | `CPU_RUN_QUEUE` | ✅ 已改（R9） | — |
| `HalCpuIsBsp` | `HalCpuIsBootstrapProcessor` | ⬜ | **PR-R-hal-bsp** |
| `Runq*` | `RunQueue*` | ⬜ | **PR-R-runq** |
| `gCmdCount` | `gCommandCount` | ⬜ | **PR-R-cmd** |
| `HalSerialInit` / `HalTimerInit` | `*Initialize` | ⬜ | **PR-R-init-hal** |
| `PhysicalMemoryInit` / `VirtualMemoryInit` | `*Initialize` | ⬜ | **PR-R-init-mm** |
| `FileSystemInit` 等 Services `*Init` | `*Initialize` | ⬜ | **PR-R-init-svc** |
| `gSaveW` / `gSaveH` | `gSaveWidth` / `gSaveHeight` | ⬜ | **PR-R-save** |
| `ToySockAddrIn` | `TOY_SOCK_ADDR_IN` | ⬜ | **不进默认**（SDK ABI，须另说） |

详细历史清单见 [`路线图.md`](路线图.md#五c-标识符命名规划) §五。

---

## 九、示例对比

### 9.1 正例

```c
/* 函数命名 */
int HalSerialWriteString(const char *Text);
void *PhysicalMemoryAllocatePages(UINT32 PageCount);
int ToyDriverRegister(const TOY_DRIVER *Driver);

/* 变量命名 */
static UINT32 gActiveWindow;
static char sBuffer[256];
void ProcessFile(int FileDescriptor, const char *FilePath);

/* 类型命名 */
typedef struct {
    UINT64 BaseAddress;
    UINT32 Length;
} MEMORY_REGION;

typedef enum {
    DRIVER_CLASS_NONE = 0,
    DRIVER_CLASS_BLOCK,
    DRIVER_CLASS_INPUT
} DRIVER_CLASS;

/* 宏命名 */
#define MAX_TASKS 8
#define PAGE_SIZE 4096
#define TOY_DEBUG 1
```

### 9.2 反例

```c
/* ❌ 缩写了不该缩写的 */
int InitSrl(const char *s);           /* 应为 InitializeSerial */
void *alloc_pages(UINT32 n);          /* 应为 PhysicalMemoryAllocatePages */
int drv_reg(const TOY_DRIVER *d);     /* 应为 ToyDriverRegister */

/* ❌ 用了 snake_case（内核 / Toy* 库） */
static UINT32 active_win;
static char buf[256];

/* ❌ 用了 camelCase */
int halSerialWrite(const char *text);

/* ❌ 变量没加 g/s 前缀 */
static int initialized;               /* 应为 sInitialized */
UINT32 activeWindow;                  /* 应为 gActiveWindow */
```

---

## 十、工具支持

### 10.1 编辑器配置

`.editorconfig`：
```ini
[*.{c,h}]
indent_style = space
indent_size = 4
```

### 10.2 检查工具（可选）

未来可以加：

- `Tools/check-naming.sh` — 检查新增代码的命名
- CI 集成：PR 提交时自动检查

**当前阶段**：靠人工 review。

---

## 十一、常见问题

### Q1: 为什么不用 snake_case？

**A**:

- PascalCase 与 EDK2 一致（UEFI 参考实现）
- PascalCase 在英文中更接近自然语言
- ToyOS 现有内核代码已经大量使用 PascalCase
- 用户态 POSIX 符号仍用 snake_case（见 §2.5）

### Q2: 为什么类型用 ALL_CAPS？

**A**:

- 与 EDK2 一致（`EFI_FILE_PROTOCOL`）
- ALL_CAPS 让类型在代码中一目了然
- 与变量区分（变量 PascalCase，类型 ALL_CAPS）

### Q3: 为什么全局变量加 `g` 前缀？

**A**:

- 一眼看出是全局变量
- 避免与局部变量混淆
- 与 EDK2 一致（`gBS` / `gImageHandle`）

### Q4: 缩写白名单怎么定？

**A**:

- 只保留**公认缩写**（如 CPU / USB / PCI）
- 不保留**可写全的缩写**（如 Init → Initialize）
- 有歧义的缩写写全（如 Cfg → Configure / Configuration）

### Q5: 旧代码什么时候改？

**A**:

- 不强制一次改完
- 改代码时顺手改
- 每次迁移独立 PR
- 参考 [`路线图.md`](路线图.md#五c-标识符命名规划) §五

---

## 十二、参考

| 来源 | 说明 |
|------|------|
| EDK2 Coding Standards | UEFI 参考实现的命名规范 |
| Linux Kernel Coding Style | Linux 内核规范（snake_case；ToyOS **不**采用） |
| ToyOS 现有代码 | `HalSerialWrite` / `VirtualMemoryInit` / `ToyDriverRegister` |

---

## 十三、修订记录

| 日期 | 说明 |
|------|------|
| 2026-09-16 | 初版：核心原则、函数/变量/类型/宏/文件命名、缩写白名单、迁移策略。对照源码修正 §8.3（R9 类型已改）；补 POSIX/CRT 与脚本例外 |
| 2026-09-16 | §8.3 对齐路线图 **R 柱 P0**（`PR-R-hal-bsp` … `PR-R-save`） |
