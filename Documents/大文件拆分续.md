# ToyOS 大文件拆分续

> **本文是本轮拆分的细则。** 排期认 [`路线图.md`](路线图.md#pr-s-split-cont) 文首 ★。  
> 统计：2026-09-19。范围：ToyKernel 自有源（不含 lwIP）与 ToyBoot。  
> 上轮盘点（目标曾放宽到约 600～800 行）见 [`Done/大文件拆分3.md`](Done/大文件拆分3.md)，**不**再当本刀队列。

相关文档：

- [`路线图.md`](路线图.md#pr-s-split-cont) — 当前刀与下一 PR
- [`开发命名规范.md`](开发命名规范.md) §6.3 — 文件名与 ≤300 行
- [`Done/大文件拆分3.md`](Done/大文件拆分3.md) — 上轮已收官的 FilesUi / Desktop / Video / GuiWm / Compose

---

## 一、门槛

| 项 | 约定 |
| --- | --- |
| 单文件 | 每个 `.c` **≤300 行**。超过不收刀 |
| 形态 | 每个功能模块 = **1 个核心文件 + 数个辅助文件** |
| 目录 | 核心与辅助 **放进同名子目录**。核心文件与目录同名 |
| 辅助文件名 | 模块名 + 能力名，PascalCase（`GuiDragComposite.c`） |
| 纪律 | 只搬家，不改行为、不改结构体布局、不改对外 API、不顺手改旧函数名 |
| 粒度 | 一个 PR 只动一个模块。不要一次把整张表做完 |
| 验证 | `./build.sh`；桌面 / 键鼠 / 控制台再 `smoke-boot`；ToyBoot 再手测一次开机到 `ToyOS ready` |

已经不超过 300 行、而且仍是单文件的，**不为建目录而搬家**。跨模块共享头留在 `Include/`（如 `GuiPriv.h`），不塞进某一个子目录。

先例：`HAL/X64/Drivers/XHCI/`、`User/Library/ToyUi/`。

`Makefile` 现在是 `wildcard Common/Services/*.c`，**不进子目录**。某个模块第一次开目录时，同一刀补上该目录的 wildcard。这只是把新文件编进来。

`GuiWm.c`（182 行）和 `GuiCompose.c`（249 行）已经达标，**不再重切**。路线图旧序「GuiWm → Compose → Console」的前两段核心已完成；本轮从仍超标的窗口旁支开始，再回到 Console。

全树仍约 **62** 个实现文件超过 300 行。`ToyBoot/Boot.c` 为 **1257** 行。

---

## 二、波次

| 波 | 范围 | 何时 | 对象 |
| --- | --- | --- | --- |
| A | 窗口旁支 | **先做（★）** | GuiDrag / GuiDraw / GuiPointer / GuiUser / GuiOpen / GuiBackup / GuiFocus |
| B | 控制台 | A 完 | Console / ConsoleCmd |
| C | 桌面与文件 | B 完 | Desktop / DesktopIcons / DesktopPaint / DesktopMenu / FilesUi* |
| D | 商店与设置 | C 完 | Store / StoreUi / StoreNet / SettingsUi / Theme / EditUi / Db |
| E | 文件系统 | 穿插 | FatDir / FatFile / FileSystem / FatPath / Gpt |
| F | 内核核心 | 穿插 | Scheduler / Process / TaskFd / Tasks / SyscallFs / VirtualMemory |
| G | HAL 与网卡 | 后置 | Video / Net / VirtioNet / E1000 / Tcp / HalSerial / 块设备 / ACPI |
| H | xHCI | 最后 | Msc / Hid / Hub / Core / Mouse / Device（BSS 敏感） |
| I | ToyBoot | 可与 A 错开 | `Boot.c` 1257 行 |
| J | 用户库 | 小刀 | `ToyUi.c` |

函数边界在该 PR 动手前再对一遍源码。下表是规划，不是已经改过的文件。

---

## 三、先做的 9 个 PR（波 A、B）

| 序 | PR | 现在 | 拆成 | 验证 |
| --- | --- | --- | --- | --- |
| **1** | **PR-S-guidrag-1** | `GuiDrag.c` 750 | `Common/Services/GuiDrag/`：核心 `GuiDrag.c`（281）+ `GuiDragComposite.c`（291）+ `GuiDragBackup.c`（212） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **2** | **PR-S-guidraw-1** | `GuiDraw.c` 668 | `Services/GuiDraw/`：核心 `GuiDraw.c`（296）+ `GuiDrawChrome.c`（276）+ `GuiDrawShadow.c`（111） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **3** | **PR-S-guipointer-1** | `GuiPointer.c` 628 | `Services/GuiPointer/`：核心 `GuiPointer.c`（244）+ `GuiClick.c`（230）+ `GuiPointerMouse.c`（185） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **4** | **PR-S-guiuser-1** | `GuiUser.c` 504 | `Services/GuiUser/`：核心 `GuiUser.c`（297）+ `GuiUserBlit.c`（216） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **5** | **PR-S-guiopen-1** | `GuiOpen.c` 483 | `Services/GuiOpen/`：核心 `GuiOpen.c`（233）+ `GuiOpenApps.c`（266） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **6** | **PR-S-guibackup-1** | `GuiBackup.c` 433 | `Services/GuiBackup/`：核心 `GuiBackup.c`（263）+ `GuiBackupSample.c`（180） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **7** | **PR-S-guifocus-1** | `GuiFocus.c` 313 | `Services/GuiFocus/`：核心 `GuiFocus.c`（213）+ `GuiFocusConsole.c`（107） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **8** | **PR-S-console-1** | `Console.c` 590 | `Services/Console/`：核心 `Console.c`（220）+ `ConsoleWrite.c`（173）+ `ConsoleInput.c`（227）；`ConsoleScroll.c`（230）移入 | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |
| **9** | **PR-S-consolecmd-1** | `ConsoleCmd.c` 695 | 同一 `Console/`：核心 `ConsoleCmd.c`（286）+ `ConsoleAlias.c`（297）+ `ConsoleBuiltin.c`（126） | build + smoke-boot **已过**；**NUC 已过**；**已 TG** |

**序 1–9 已 TG（NUC 已过，2026-09-19）。** 序 9 两文件放不下，多了 `ConsoleBuiltin.c`。`StrEq` / `FindCommandIndex` / `FindUserAliasIndex` / `CommandAlias` / `CommandUnalias` 去掉 `static`，仍是这一处定义。波 A/B 已收口。**`PR-S-desktop-1` 已 TG**（2026-09-19）。`FillRectFreeAlpha` 仍 static，跟任务栏绘制走。**`PR-S-filesui-1` 已 TG**（2026-09-19）。`DrawLine` 跟绘制走。**`PR-S-store-1` 已 TG**（2026-09-19，只拆 catalog）。**`PR-S-store-2` 已 TG**（2026-09-19，只拆安装）。`CopyStr` / `JoinPath` 在 `StorePriv.h` 里是 `static inline`。**`PR-S-store-3` 已 TG**（2026-09-19，只拆组合包）。**`PR-S-store-4` 已 TG**（2026-09-19，只拆卸装）。**`PR-S-store-5` 已 TG**（2026-09-19，只拆查询）。两文件才放下，多了 `StoreManaged.c`。`StrEqIgnoreCase` 在 `StorePriv.h` 里是 `static inline`。**`PR-S-store-6` 已 TG**（2026-09-19，只拆共享帮手）。商店目录内每个 `.c` 均 ≤300。`StrEq` 在 `StorePriv.h` 里是 `static inline`。**`PR-S-settingsui-1` 已 TG**（2026-09-19）。`SettingsUiPriv.h` 写全；布局与 FilesUi 撞名的符号改成 `gSet*`。**`PR-S-theme-1` 已 TG**（2026-09-19）：核心 `Theme.c`（254）+ `ThemeLive.c`（63）+ `ThemeParse.c`（217）+ `ThemeCfg.c`（137）+ `ThemeSave.c`（232）；`ThemePriv.h` 写全；原 static `gUiScale` 与 Video 撞名改为 `gThemeUiScale`，`NormalizeUiScale` 在 priv 里 `static inline`。build + smoke-boot 已过。**`PR-S-storeui-1` 已 TG**（2026-09-19）：核心 `StoreUi.c`（71）+ `StoreUiModel.c`（167）+ `StoreUiPaint.c`（276）+ `StoreUiInput.c`（235）；`StoreUiPriv.h` 写全。与 Settings/Files 撞名的符号改为 `gStoreUi*` / `StoreCatLabel` / `StoreSetStatus` / `StorePaintList`。`StrEq` 在 priv 里是 `static inline`。`DrawDetail` 仍 static，跟绘制走。build + smoke-boot 已过。**`PR-S-editui-1` 已 TG**（2026-09-19）：核心 `EditUi.c`（150）+ `EditUiPaint.c`（161）+ `EditUiInput.c`（136）。与 Gui/Video/Console/Files 撞名的符号改为 `gEditSaveX` / `gEditSaveY` / `gEditDirty` / `gEditLen` / `gEditStatus` / `EditSetStatus` / `EditClampScroll`。`CopyStr` 在 priv 里是 `static inline`。`DrawLine` 仍 static，跟绘制走。build + smoke-boot 已过。**`PR-S-storenet-1` 已 TG**（2026-09-19）：核心 `StoreNet.c`（184）+ `StoreNetHttp.c`（261）+ `StoreNetParse.c`（151）。`HttpGet` 与正文解析分开，否则单文件超 300。build + smoke-boot 已过。**`PR-S-db-1` 已 TG**（2026-09-20）：核心 `Db.c`（252）+ `DbFile.c`（175）。`IsSpace` / `KeyOk` / `CopyStr` / `StrEq` 在 `DbPriv.h` 里是 `static inline`。`FindSlot` / `AllocSlot` / `ClearAll` / `ImportThemeCfgIfEmpty` 去掉 `static`。原 static `gDirty` 与 Video 撞名改为 `gDbDirty`。`ApplyLine` 仍 static，跟读盘走。build + smoke-boot 已过。**`PR-S-fatdir-1` 已 TG**（2026-09-20）：`Library/Fat/` 核心 `FatDir.c`（164）+ `FatDirScan.c`（243）+ `FatDirName.c`（235）+ `FatDirSlot.c`（276）+ `FatDirStress.c`（188）。函数名未改。build + smoke-boot 已过。**`PR-S-fatfile-1` 已 TG**（2026-09-20）：`Library/Fat/` 核心 `FatFile.c`（122）+ `FatFileRead.c`（103）+ `FatFileWriteAt.c`（245）+ `FatFileWrite.c`（217）+ `FatFileDelete.c`（248）。`FatIoBreath` / `SeekFileOffset` 去掉 `static`。build + smoke-boot 已过。**JX 下一刀只做 `PR-S-filesystem-1`**（收拢 `FileSystem.c`，尚未开始）。

序 9 的切块：

| 文件 | 放什么 |
| --- | --- |
| `ConsoleCmd.c` | 命令表、Register、分发：`ConsoleRegister` / `ConsoleRunLine` |
| `ConsoleAlias.c` | 用户别名：`ConsoleUserAliasLoad` / `CommandAlias` / `CommandUnalias` |
| `ConsoleBuiltin.c` | 内置：`CommandHelp` / `CommandClear` / `CommandEcho` / `ConsoleRegisterBuiltins` |

序 8 的切块：

| 文件 | 放什么 |
| --- | --- |
| `Console.c` | 焦点、提示符、重画：`Prompt` / `ConsoleFocusLoad` / `ConsolePaintShellWindow` |
| `ConsoleWrite.c` | 输出：`ConsoleWrite` / `ConsoleDrawString` / `ConsoleWriteHex*` |
| `ConsoleInput.c` | 按键与串口壳：`ConsoleOnChar` / `ConsoleOnEnter` / `ConsoleSerialRun` |
| `ConsoleScroll.c` | 原样移入，不改行缓冲 |

序 7 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiFocus.c` | 焦点裁剪与光标：`GuiFocusClient` / `GuiFocusApply` / `GuiFocusClearClient` / `GuiFocusHome` |
| `GuiFocusConsole.c` | Shell 输入行：`GuiConsolePull` / `GuiConsolePush` / 提示符 / `GuiShellAcceptsInput` |

序 6 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiBackup.c` | 备份缓冲：分配、`BackupWindowAt`、从备份贴窗、`GuiBackupFocusWindow` |
| `GuiBackupSample.c` | 像素采样与覆盖：`AnalyticWindowPixel` / `SampleWindowBackupPixel` / `PixelCoveredByHigherWindow` / `GuiBackupSyncRect` |

序 5 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiOpen.c` | 关窗、槽位、摆放，以及开窗辅助 `OpenChromeDefer` / `OpenFadeIn` / `FocusExistingKind` |
| `GuiOpenApps.c` | `GuiOpenShell` / `GuiOpenSettings` / `GuiOpenStore` / `GuiOpenFiles` / `GuiOpenEdit` |

序 4 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiUser.c` | 开窗、按钮、轮询：`GuiOpenUser` / `GuiUserAddButton` / `GuiPollUserInput` / `RepaintUserWindow` |
| `GuiUserBlit.c` | 客户区绘制与像素：`PaintUserClient` / `GuiDamageUser` / `GuiDamageRectUser` |

序 3 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiPointer.c` | 队列轮询与锁：`GuiPollMouse` / `GuiPollMouseMotion` / `GuiInputLock` |
| `GuiClick.c` | 按下命中：`GuiHandleClick` |
| `GuiPointerMouse.c` | 分辨率钳窗、方向键、右键、`GuiOnMouse` 边沿 |

序 2 的切块：

| 文件 | 放什么 |
| --- | --- |
| `GuiDraw.c` | 绘制入口：标题色、悬停、`SyncWindowVisuals`、`DrawWindowAt` / `DrawWindowAtEx`、桌面裁剪填充、`WindowOccludedByOther` |
| `GuiDrawChrome.c` | 标题栏与关闭钮：`DrawWindowChromeAt`、遮挡画线、`DrawCloseButton` |
| `GuiDrawShadow.c` | 右下阴影：`DrawWindowShadowAt`、`ExpandRectByWindowShadow`（`ShadowUnderPixel` 仍 static） |

序 1 的切块（动手前再对函数行号）：

| 文件 | 放什么 |
| --- | --- |
| `GuiDrag.c` | 拖放生命周期：`GuiDragUpdate` / `GuiDragEnd` / `MoveWindowTo` / `ClampWindowPos` / `ResetDragState` |
| `GuiDragComposite.c` | 像素合成与脏区：`CompositeDragPixel` / `CompositeDragDirtyRegion` / 脚印清除与重画 |
| `GuiDragBackup.c` | 拖动备份缓冲：`EnsureDragDirtyBuf` / `EnsureUnderDragBuf` / `CaptureDragRestoreData` / `BeginDragBackups` |

---

## 四、后面各目录

| 波 | 模块 | 行数（2026-09-19） | 切法 |
| --- | --- | ---: | --- |
| C | `Services/Desktop/` | `Desktop.c` 896 起 | **已 TG**：核心 `Desktop.c`（260）+ `DesktopGeom.c`（168）+ `DesktopIconDrag.c`（68）+ `DesktopSample.c`（218）+ `DesktopClick.c`（248）；`DesktopIcons.c`（229）+ `DesktopIconLayout.c`（212）；`DesktopMenu.c`（278）；`DesktopPaint.c`（146）+ `DesktopTaskbar.c`（271）；`DesktopNetTray.c` / `DesktopWallpaper.c` 原样移入 |
| C | `Services/FilesUi/` | 618 / 480 / 407 | **已 TG**：核心 `FilesUi.c`（294）+ `FilesUiClick.c`（177）+ `FilesUiKeys.c`（150）；`FilesUiNav.c`（276）+ `FilesUiPreview.c`（136）；`FilesUiPaint.c`（201）+ `FilesUiList.c`（291）；`FilesUiActions.c`（198）原样移入 |
| D | `Services/Store/` | `Store.c` 1770 | **已 TG**（共享帮手）：核心 `Store.c`（200）+ `StoreDepends.c`（198）+ `StoreDb.c`（73）；另有 Catalog 203 / Install 262 / Combo 201 / Remove 205 / Query 278 / Managed 154。`StrEq` 在 `StorePriv.h` 里是 `static inline` |
| D | SettingsUi / Theme / StoreUi / EditUi | 1011 / 905 / 774 / 467 | **SettingsUi 已 TG**。**Theme 已 TG**。**StoreUi 已 TG**。**EditUi 已 TG**：核心 `EditUi.c`（150）+ `EditUiPaint.c`（161）+ `EditUiInput.c`（136）；撞名改为 `gEdit*`。**Db 已 TG** |
| D | StoreNet / Db | 607 / 470 | **StoreNet 已 TG**：核心 `StoreNet.c`（184）+ `StoreNetHttp.c`（261）+ `StoreNetParse.c`（151）。**Db 已 TG**：核心 `Db.c`（252）+ `DbFile.c`（175） |
| E | Fat / FileSystem | 1076 / 910 / 835 | **FatDir 已 TG**。**FatFile 已 TG**。下一刀 `FileSystem.c`（835） |
| F | Scheduler / Process | 978 / 693 | `Core/Scheduler/`、`Core/Process/`。`SchedRunq.c` / `SchedWait.c` 移进 Scheduler 目录，不重写 |
| F | TaskFd / Tasks / SyscallFs / VirtualMemory | 602 及以下 | 描述符按种类切；仅仍超 300 的才动 |
| G | Video / Net | 950 / 761 | `Drivers/Video/`、`Drivers/Net/` |
| G | AcpiMadt | 1065 | MADT 解析 vs 电源，放 `HAL/X64/` 下子目录或同目录辅助文件 |
| G | VirtioNet / E1000 / Tcp / HalSerial / Nvme / Ahci | 769～497 | 各一刀，不与功能改动混 |
| H | `Drivers/XHCI/` | 1030～548 | **目录已在**。只在目录内再切。禁止搬 DMA 环和 BSS 布局 |
| I | ToyBoot | `Boot.c` 1257 | 见第五节 |
| J | `User/Library/ToyUi/` | `ToyUi.c` 360 | 目录已在。同夹加 `ToyUiWidgets.c`（复选框 / 列表 / 输入框） |

---

## 五、ToyBoot

ToyBoot 目录本身就是模块夹，新文件放在 `Boot.c` 旁边，**不再套** `ToyBoot/Boot/`。

| 文件 | 放什么 | 现在 |
| --- | --- | --- |
| `Boot.c` | 核心：`UefiMain`、跳转内核 | 1257，要瘦 |
| `BootVideo.c` | 分辨率、EDID、`THEME.CFG` | 从 `Boot.c` 搬出 |
| `BootKernel.c` | 找盘、读 ELF、装载 | 从 `Boot.c` 搬出 |
| `BootPci.c` | xHCI 基址 | 从 `Boot.c` 搬出 |
| `BootSerial.c` | 串口 | 111 行，留在同一夹 |

现有函数是 UEFI 词序（如 `GetAndSetVideo`）。本刀只搬家，不改名。

---

## 六、明确不动

| 类 | 路径 | 原因 |
| --- | --- | --- |
| 字体点阵 | `Fonts/terminus*`、`cjk16`、`FontRegistry` | 数据表，切开没有功能边界 |
| 第三方 | `ThirdParty/lwip`、`HAL/X64/LwIp` 里的协议栈本体 | 保持上游风格 |
| 已达标核心 | `GuiWm.c` 182、`GuiCompose.c` 249、`GuiHit` / `GuiFade` / `GuiResize` / `GuiCursor`、`ConsoleScroll.c` | 不再重切。`ConsoleScroll.c` 只在 Console 开目录时移入 |
| 构建与文档 | `Makefile` 主体逻辑、脚本、`Documents/` | 不是运行逻辑。子目录 wildcard 除外 |
| 异常向量表 | `Arm64/Vectors.S` 一类 | 除非确认是可拆的逻辑，否则不动 |

---

## 七、每刀验收

1. 本刀碰到的每个 `.c` ≤300 行。
2. `./build.sh` 成功；镜像里的内核已换。
3. 窗口 / 桌面 / 控制台：`smoke-boot` 到 `ToyOS ready`。拖窗、叠窗、残影的观感验收在 **NUC**，不在 QEMU（QEMU 性能太差，残影判断不靠谱）。
4. 串口无新的 `EXCEPTION`。
5. 不夹带功能改动。旧符号名保持。
