# ToyOS 当前进展

日期：2026-09-05

排期与验收表以 [`路线图.md`](路线图.md) 为准（未实现在前、归档只追加）。分层硬规则以 [`架构分层.md`](架构分层.md) 为准。文件树与启动顺序见 [`结构说明.md`](结构说明.md)。本文是快照：现在课堂上能跑什么、已经归档了哪些大块、**1.2f 已收尾**、下一阶段 **1.3*** 做什么、后面还有哪些真正的大方向。

---

## 1. 现在能干什么（产品 / 课堂）

**课堂主路径是 x86 QEMU + ToyImage**，不是「三架构同一套桌面」。加电链路是：OVMF（UEFI）→ ToyBoot（`BOOTX64.EFI`）→ `Kernel.elf` → 模块表 → GOP 桌面 / Shell。学生能看见固件把控制权交给引导、引导把 ELF 内核搬进内存、内核开分页/中断/调度、用户 ELF 经 syscall 读写文件与控制台。入口：`ToyImage/run-split.sh`（盘 0 = ESP，盘 1 = `rootfs/`）；无头冒烟 `smoke-boot.sh`。

这条路径上已经能用的东西：

| 面 | 现状（归档级） |
|----|----------------|
| 桌面 | GOP 帧缓冲；标题栏 / 拖动 / 重叠合成；桌面图标双击开 Shell、Settings、Files |
| 主题 | `TOYOS.DB` KV + 仍写 `THEME.CFG`（Boot 读 `mode=`）；颜色 / 点阵字体 / 分辨率偏好（改分辨率须退出 QEMU 再跑脚本） |
| 存储 | Block + GPT + FAT（8.3+LFN、`.`/`..`、`mkdir`/`rmdir`）；多卷前缀 `TOYOS:` / `ESP:` / `A:`；识盘靠卷上 `TOYOS.ID` |
| 文件浏览器 | 进目录、预览、`.ELF` 打开；删除 / 新建 / 重命名 |
| 用户态 | Ring 3 ELF：`fork`（COW）/`wait`/`execve`/`pipe`/`dup`/`brk`/`kill`；CRT 最小集 + `CAT`/`WRITE`；简易 `.so`（JUMP_SLOT） |
| 语言 | UTF-8 绘制 + 教学用 CJK 点阵子集；UI 字符串表 `lang=en\|zh` 可持久化 |
| 网络 | virtio-net；builtin UDP/TCP（ping / echo / listen）；`LWIP=1` 时用户 socket（`NETDEMO`/`NETSRV`） |
| SMP | 演示级：AP 拉起、每核 idle、可偷 READY 任务；**shell / gui 钉在 BSP** |

**Arm64 / RiscV 是另一条 virt 骨架，不是 ToyImage / OVMF。** 入口是 `./build.sh arm64|riscv` + `run-virt-arm.sh` / `run-virt-riscv.sh`，QEMU `-kernel`（自有 Boot：DTB / OpenSBI）。**1.2d 已齐**：ramfb + virtio-input + virtio-blk + 同一套 Common Gui；默认开窗口（无 `DISPLAY` 回退 `--headless`）；`smoke-virt.sh` 无头冒烟。串口子集仍可用 `TOY_VIRT_SERIAL=1`。**1.2e N9/N10 ✅**：桌面挂 `net` + 默认 virtio-net + `ping 10.0.2.2`。**1.2f A10～A15 ✅**：真 MMU；用户态/syscall；本 arch `HELLO.ELF`；真 IRQ/timer；virt `HalSmp*`；HalFrame 命名中立。

已知课堂边角：QEMU 改分辨率须退出重跑；宿主极忙时 AP 仍可能超时（已 `--kill-qemu` / 单核降级 / Guest 单核继续）。

---

## 2. 已归档的大块（按阶段 / PR 族）

细表与验证命令在路线图文末，这里只按族归并，方便判断「哪一层已经能讲、哪一层还缺一块」。

| 族 | 覆盖 | 课堂上对应什么 |
|----|------|----------------|
| **内存 / syscall** | 阶段 1～2：分页、Ring 3、`int 0x80` + `syscall`/`sysret`、独立页表、COW fork、ELF、`open/read/write/close` | `exec HELLO.ELF` / `FORK.ELF` / `WAITNH.ELF` / `DYNDEMO.ELF` |
| **GUI 桌面** | G1～G10 窗口与合成；D1～D7 字体 / Theme / 图标 / Settings / `THEME.CFG` / GOP `mode=`；**G11** Settings/Files 浅接入 | 能开窗拖动、改色改字体、双击图标；Settings 有框、Files 空目录可见 |
| **存储 FAT** | Block/ATA PIO、GPT、FAT 读写、FS1 语义、FS2 多卷 | `ls` / `write` / `mkdir` / `vols` / `ls TOYOS:` |
| **设置库** | DB1：`TOYOS.DB`；Theme 优先 DB，仍写 CFG 给 Boot | `dbget` / `dbset`；Settings 改色后两份都更新 |
| **文件浏览器** | FB1 只读+打开；FB2 删/建/改名 | 桌面 Files 或 `files` |
| **CRT** | CRT1 最小 libc 形状；CRT2 文件 API/`errno` | `HELLO.ELF` 改 C；`CAT.ELF` / `WRITE.ELF` |
| **I18N** | I18N1 字形/UTF-8；I18N2 字符串表 + `lang` | `lang zh`；桌面标签中文 |
| **HAL 门面** | PR-1～6：Common 目录、`Include/`、`BOOT_INFO`、设备门面、去 x86 对外命名 | Common 不 include `Drivers/` |
| **架构解耦 A1～A9** | 封死 `Arch*`、任务帧、页表/ELF 按 arch、清 Common 内联 asm、virt 链 `KernelMain`、串口子集 Shell | x86 回归不破；virt 串口 `ToyOS ready` |
| **用户态成系统 P1～P4** | `execve` / `pipe`+`dup` / `brk` / 简单 `kill` | `EXECDEMO` / `PIPEDEMO` / `BRKDEMO` / `KILLDEMO`（**x86 FAT**） |
| **网络 N1～N8** | virtio-net + builtin + 可选 lwIP socket | `ping`；`LWIP=1` 时 `exec NETDEMO.ELF` |
| **x86 SMP S1～S4** | MADT/SIPI、每核 timer、自旋锁、READY 偷任务 | `-smp 2` 见 `idle0`/`idle1`；**演示级，不是真并行桌面** |
| **跨架构 Boot V1** | Arm DTB / RiscV OpenSBI → `BOOT_INFO` | virt `mem`/`boot` 与 `-m` 一致 |
| **virt 桌面 V2～V6** | ramfb → `HalVideo`；virtio-input；virtio-blk→FAT；Common Gui；`run-virt-*.sh` / `smoke-virt.sh` | QEMU 窗口同一套桌面；**非** OVMF。尚无 virt 网 |

旁路但已归档：FS1/FS2、Q1（QEMU/SMP 启动稳健性）、L0（本文依赖的分层说明书）。

---

## 3. 分层原则（已写入文档的理想）

依赖只允许向下。硬规则已经写进 [`架构分层.md`](架构分层.md)，实现时按回归看，不要把驱动头漏进 Common；UEFI 类型只留在 x86 Startup。

```
User → (syscall) → Core / Services
Services → Core / Library / Font / Hal*
Core → Library / Font / Hal* / BOOT_INFO
HAL/<Arch>/ 实现 + Drivers（私有头不进 Include/）
Boot：各 Arch 自有路径 → Startup 填 BOOT_INFO
```

- **HAL** 收纳全部架构代码（GDT、页表位、PL011 vs COM1、GIC vs LAPIC、ramfb vs GOP）。
- **Common 只消费 `BOOT_INFO` + `Hal*`**，禁止 `#include HAL/<Arch>/Drivers/*.h`，禁止写死 `EM_X86_64` / UEFI 类型。
- **Boot**：x86 继续 UEFI + ToyBoot + OVMF；**Arm64 / RiscV 自有 Boot**（virt + DTB / OpenSBI `-kernel`）。
- 相同 GUI = **Common Gui**。亮屏是该 Arch 的 **HAL Video**（virt 上 ramfb 或 virtio-gpu，不是 GOP）。块设备在该 Arch HAL 注册 virtio-blk，再接已有 Common FAT。

模块主线（x86 全表）：`serial → mem → vmm → video → cpu → smp → fs → usb → net → gui → sched → console`。Video 先于读库；识盘（`TOYOS.ID`）≠ 数据库（`TOYOS.DB`）。virt 桌面：video / fs / **net（N10）** / 输入 / gui / **smp（A14）**；串口子集亦挂 smp。

---

## 4. 正在 / 下一步（1.3*）

**一句话**：**1.2e** / **1.2f**（A10～**A15**）已齐。下一程产品愿景：**1.3g**（G12 控件化）或 **1.3d/f/l**。验收仍走 `run-virt-*.sh` / x86 `smoke-boot.sh`。

| PR | 名称 | 状态 | 要交付什么 |
|----|------|------|------------|
| **N9～P4** | 1.2e 产品刀 | ✅ | virt 网 / FAT 8MiB / GUI 浅接入 / 简单信号 |
| A10 | Arm/RiscV 真 MMU | ✅ | `HalPagingEnable` 开硬件；缺页可观测 |
| A11 | 用户态 + syscall 帧 | ✅ | `HalUser*` / `HalFrameSyscall*` + 内嵌自测 |
| A12 | HalElf reloc | ✅ | 本 arch 用户 ELF / virt `exec HELLO.ELF` |
| A13 | 真 IRQ / timer | ✅ | GIC CNTV / SBI timer；`HalCpuHalt`→WFI |
| A14 | virt `HalSmp*` | ✅ | PSCI / soft-release；`-smp 2` 见 `idle1` |
| A15 | HalFrame 命名 | ✅ | `InstructionPointer`/`StackPointer` + `GetInstructionPointer` / `GetArgument*` |

**节奏**：下一刀 **1.3g G12**（或其它 1.3 柱）。

本阶段明确不做：真机 / NVMe / 安装器、完整 POSIX / `mmap` / 信号全集、TTF。

---

## 5. 后面要完善的大方向

下列是**产品级大块**，不是把路线图 1.3「可穿插」逐条拆成 PR。1.2d 已做完 virt 桌面；**1.2e** 只切其中最高杠杆的一刀（virt 网 + FAT 大文件 + GUI 浅接入 + 简单信号）。课堂主路径仍会长期停在这些缺口上。每块都写：现状 / 缺口 / 为何够大。

### 5.1 GUI（控件、视觉、热分辨率）

**现状**：x86 上已经有即时绘制的窗口管理器（`Gui.c`）：最多 6 窗、标题栏 40px、拖动与重叠合成、桌面三图标（Shell / Settings / Files）、Settings 文字菜单、Files 列表。点阵字体 + 教学 CJK 子集；颜色是 `0x00RRGGBB`。`UI.c` 里已有按钮/进度条原语；**G11** ✅ 已浅接入 Settings 边框与 Files 空状态/焦点行，**未**全面控件化。无开始菜单、无图标拖放、无壁纸位图、无 TTF。

**缺口**：把 Settings / Files / Shell 从「能用的教学窗」做成可讲的桌面产品，需要布局与状态（悬停、焦点、无图标占位），而不是再堆几个 `FillRect`。**1.2e G11** ✅ 浅接入已做；控件库全面接入（**G12**）、运行中改分辨率（virtio-gpu 等，不替代 D7 的写盘重启）仍是后段。virt 上同一套 Gui 已属 **1.2d V5**；GUI 大块剩下的是视觉与交互，不是「再移植一套窗口」。

**为何是大块**：绘制路径绑在内核服务线程上（`GuiTask`），没有用户态 toolkit，没有矢量字。改一处颜色容易，改信息架构（多窗、菜单、文件预览排版）会碰到合成、剪裁、备份脏区、点阵度量，必须按屏给规格，不能当普通 Web UI 迭代。

### 5.2 网络

**现状**：x86 virtio-net 已能 ping、UDP、简陋 TCP echo；`make LWIP=1` 后用户态 socket（号 8～12）可跑 `NETDEMO`/`NETSRV`。`run-split.sh` 已 `hostfwd` 9000。Builtin 是教学栈；lwIP 是可选生产路径。

**缺口**：默认构建仍是 builtin（lwIP 要显式打开）；无 DNS；socket 错误码不完整。virt **N9/N10** ✅：`HalNet` + 桌面挂 `net` + `ping 10.0.2.2`；文档已写清 `LWIP=1`（不强改默认）。用户程序不能当「小 Unix 网络课」的默认体验，除非课堂构建显式 `LWIP=1`。

**为何是大块**：协议正确性、RX 与调度、用户 ABI 仍是长线。N9/N10 只做 HAL + 挂网 + ping；继续补 DNS/错误码仍可穿插，不和本阶段抢同一刀。

### 5.3 硬盘支持

**现状**：x86 上 ATA PIO（Primary 主/从），Common 已有 Block / GPT / FAT；写文件上限 **8MiB**（**FS3** ✅）；多卷与只读 ESP 已做。课堂盘是 QEMU 的 vvfat / raw，不是安装器。

**缺口分两层**。（1）**virt 块设备** = 1.2d **V4** ✅：Arm/RiscV HAL 已注册 virtio-blk，盘面复用 `ToyImage/rootfs` 那套 FAT+`TOYOS.ID`。（2）**真硬盘**：NVMe / AHCI、安装器、真机分区，路线图明确后置。FAT 大文件与写健壮性已由 **FS3** 收口；VFS/`stat`/第二后端见 **1.3f**。

**为何是大块**：Block 抽象已经在，缺的是**后端与启动介质故事**。V4 已让 virt 上 `ls`/`exec` 命令成立（本 arch 用户 ELF 仍待 MMU）；真机还要把固件、ESP、系统卷、写盘工具串起来。混在一个 PR 里会把「能 ls」和「能装机」缠死。

### 5.4 开发库（用户态 libc）

**现状**：`User/crt/` 是教学 CRT，不是 glibc：`printf` 子集、`malloc` 走 `brk`、`open/read/write/close`+`errno`、`fork/wait/pipe/dup/execve`。头文件在 `User/include/`，禁止链内核 `Common/`。动态库只有 JUMP_SLOT 的 `LIBTOY.SO`。

**缺口**：没有 `mmap`、没有信号全集（**1.2e P4** ✅ 已做简单 `kill`）、没有完整 stdio 缓冲与 locale、没有线程、`.so` 无 TLS/COPY。任务槽约 8；FD 打开时整文件读入。POSIX 课上能演示的是「syscall 形状」，不是「把 Linux 用户程序直接编过来」。

**为何是大块**：每加一类 API 都要内核 syscall + 页表/信号语义 + CRT + 演示 ELF + FAT 同步。P1～P3 已经把「成系统」的最小集做完；再往下是完整 POSIX，应单独当大方向，不要假装 CRT2 之后 libc 就齐了。

### 5.5 真 SMP

**现状**：x86 S1～S4 演示级——AP 活着、每核 tick、调度大锁、可偷任务。shell/gui **钉 BSP**（输入同步未做）。**virt A14** ✅：Arm PSCI / RiscV soft-release，`-smp 2` 见 `idle0`/`idle1`（演示级，不迁 shell/gui）。宿主忙时 x86 AP 超时已降级，不是「多核桌面稳定」。

**缺口**：减弱大锁 / 优先级；把 `shell`/`gui` 迁到 AP；真机 IOAPIC / 中断路由。1.2d / **1.2e** 都不要求迁 AP。

**为何是大块**：教学上「看见 idle1」已经够讲 SIPI；真并行会碰到输入队列、合成锁、用户页表跨核。和 virt 亮屏不是同一阶段。

### 5.6 进程 / POSIX 子集（信号、`mmap` 等）

P1～P3 已从「完整 POSIX」里拆出 `execve` / 管道 / `brk`。**1.2e P4** ✅ 再拆出简单信号（`kill` + 默认终止）。留下的是：`mmap`、信号全集、swap、更多任务槽与真正的 FD 流式读写。这是用户态课的再下一台阶，依赖页表与调度，而不是再写几个演示 `*.c`。

### 5.7 真机

virt 上的显示 / 输入 / 盘由 **1.2d** 覆盖（ramfb/virtio-*）。网卡由 **1.2e N9/N10** 覆盖。真机是另一条故事：x86 上 NVMe/AHCI、固件与 ESP 布局、安装器；Arm/RiscV 上真实 SoC 的 Boot 与外设，而不是 QEMU `virt` 模型。课堂验收在 QEMU 过关之前，不要把真机塞进 1.2e。

---

## 6. 给 AI 的 UI 设计材料怎么给

内核 GUI 不是浏览器，也不是能加载 TTF 的桌面环境。绘制在 **`Gui.c` / `Desktop.c` / `SettingsUi.c` / `FilesUi.c`**，经 `HalVideo*` 写帧缓冲；`UI.c` 有几何原语但多数屏仍是立即模式的填矩形 + 点阵字。典型分辨率 **1280×720**（Settings 还可 800×600 / 1024×768 / 1600×900）；标题栏 **40px**；桌面图标 **48×48**、间距 28；最多 **6** 个应用窗；颜色 **`0x00RRGGBB`**（与 `UI.h` 的 `COLOR_*`、Theme 桌面/Shell 底色一致）。字体是注册表里的点阵（`FONT_FACE`），汉字走 **`Fonts/cjk16.c` 子集**，没有矢量、没有完整 Unicode。改分辨率走写盘 + 重启（QEMU 须重跑脚本），不要假设运行中热切换已经存在。

**最稳的组合（不要只给一种）：**

1. **标注图 / 截图（PNG 或 WebP）**  
   至少一张**整屏 1280×720** 布局：桌面底色、图标网格位置、任务外的空白、Shell / Settings / Files 窗的默认位置与大小。在图上画框写字：标题栏高度、客户区边距、列表行高、关闭钮。Agent 能直接看图；只丢 Figma 云端链接而不导出，仓库里经常打不开。

2. **短文字规格（Markdown 列表或小表，大约 20 行就够）**  
   写清：目标分辨率；色值（例如桌面 `0x00404040`、标题栏焦点/非焦点各一色）；字体约束（只用现有点阵 / 若需新字必须给码点列表，不能写「用思源黑体」）；分屏：**桌面 / Settings / 文件 / Shell 窗** 各有什么控件、对齐、间距。Theme 已是 `0xRRGGBB`，不要改成 CSS `rgb()` 当唯一来源。

3. **状态**  
   默认 / 悬停 / 按下 / 窗口焦点 / 非焦点 / **无图标或空目录**。一张海报只有「漂亮的默认态」，实现时悬停和空列表会瞎编。缺状态就再补小图或在规格里用两行描述。

4. **不要当作唯一来源的东西**  
   完整 PSD/多层源文件（Agent 抽不出像素网格）；「按这个 SVG/TTF 直接进内核」；只有情绪板、无尺寸；把 Web/Material 组件库当可粘贴代码。矢量可以当**设计意图**，落地仍是矩形 + 现有字形。

5. **放哪**  
   图放进仓库（建议 `ToyKernel/docs/ui/`，文件名带屏名：`desktop-1280.png`、`settings-font.png`）或直接贴进 Cursor 对话。**图 + 约 20 行规格** 比单独一张海报有用；只给文字没有图，布局比例容易偏。改的是内核 C，一次只指定一屏（先桌面再 Settings），并写明「不要引入 TTF / 不要新建用户态 GUI 进程」。

课堂如果只是改 Theme 色和文案：一张标注过的桌面截图 + 色值表即可。如果要动 Files 列表或 Settings 分页：必须给该窗客户区的网格和空状态，否则会和现有 `GuiCompose` / 窗备份路径打架。
