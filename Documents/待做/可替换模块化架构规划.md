# ToyOS 可替换模块化架构规划

> **状态**：第 2 刀已落地（2026-09-24）。★ = `PR-SCHED-test`。排序与选核在 `SchedulerRoundRobin.c`。锁、队列、steal、`PickNext` 留在 `SchedulerRunq.c`。  
> **目标**：每个核心模块可被学生替换并验证；默认实现行为不变。  
> 相关：[`路线图.md`](../路线图.md) · [`技术手册.md`](../技术手册.md) · [`开发/开发命名规范.md`](../开发/开发命名规范.md)

---

## 一、目标与硬约束

### 目标

1. **接口与实现分离**：接口在 `Include/`（稳定）；可替换实现在 `Common/Modules/`。
2. **编译时选择**：Makefile 变量选实现（如 `SCHEDULER=round-robin`）。
3. **统一测试**：同一套测试验证默认实现与学生实现。

### 硬约束

| # | 约束 |
| - | ---- |
| 1 | **不改**对外现有 API（`SchedulerCreate` / `SchedulerYield` / `SchedulerOnTimer` 等签名不变） |
| 2 | 默认实现行为与现网一致 |
| 3 | 每刀独立可编译、可测试 |
| 4 | 新 `.c` ≤300 行 |
| 5 | 学生实现**不**编入默认 Kernel |
| 6 | 三架构可编（x86_64 / arm64 / riscv） |
| 7 | **不搬** BSS 敏感全局（`gTasks` / `gRunQueue` / `gIdleSlot` 留在框架） |
| 8 | 阶段 1 分 4 刀；一次只做一刀 |

---

## 二、三阶段总览

| 阶段 | 模块 | 说明 |
| ---- | ---- | ---- |
| **1** | 调度器（首选） | 逻辑清晰、接口小、易测；政策在 Runq |
| **2** | 内存分配器 | 同调度器模式 |
| **3** | 文件系统 | `FS_OPS` 已存在，改为编译期可选 |

阶段 1 确认后再动代码；2/3 仿阶段 1。

---

## 三、阶段 1 · 调度器（分析）

### 3.1 现有 `Scheduler` 结构

调度器已按文件拆开（约 1.8k 行），**尚无** Ops 表：

| 文件 | ~行 | 职责 |
| ---- | --: | ---- |
| `Core/Scheduler/Scheduler.c` | 291 | Init / Create / 优先级·亲和 / `ActivateTask` |
| `Core/Scheduler/SchedulerRunq.c` | 258 | **就绪队列、入队排序、偷任务、`PickNext`/`PickHomeCpu`** |
| `Core/Scheduler/SchedulerSignal.c` | 261 | `SchedulerOnTimer` → `PickNext` |
| `Core/Scheduler/SchedulerUser.c` | 285 | fork / yield / kill |
| `Core/Scheduler/SchedulerWait.c` | 293 | wait / zombie / exit |
| `Core/Scheduler/SchedulerSleep.c` | 90 | sleep 唤醒 |
| `Core/Scheduler/SchedulerBoot.c` | 193 | idle / `SchedulerStart` / 钉核 |
| `Include/Scheduler.h` | 134 | 对外 ABI |
| `Include/SchedulerPrivate.h` | 54 | 内部：`gTasks`、runq 符号、锁 |

对外入口在 `Scheduler.h`（Create、OnTimer、Yield、Sleep、Wait、Fork…）。**没有**名为 `Schedule` 的函数；真正「调度决策」是 `PickNext`（由 `SchedulerOnTimer` / `SchedulerYield` 调用）。

**政策（进 Ops，可替换）** vs **框架（不动语义）**：

| 政策 | 框架 |
| ---- | ---- |
| `Init` | `TASK` / `gTasks` 槽位 |
| `Enqueue`（排序） | Create / fork / wait / signal / sleep |
| `Remove` | `ActivateTask`、页表、syscall、HAL 定时器 |
| `PickHome` | SMP 锁；`SchedulerStart` 钉核 |
| `PickNext`（本核选任务；返回空则框架选 idle） | **idle**（`gIdleSlot`，没有 `gIdleTask*`） |
| | **steal**（v1 留在 `PickNext` 框架侧） |
| | **`Ticks`**（timer 路径 +1，政策只读） |

调用约定：`Enqueue`/`Remove` 由框架持 `gRunQueueLock`，实现不再加锁。`PickNext` 返回空 = 无任务，框架用 `IdleTaskForCpu`。

`SchedulerRunq.c` **不能整文件当政策**。`RunQueueEnqueueLocked` 是排序（政策），但 `PickNext` 同时做了锁、steal、`gIdleSlot` 兜底（框架）。第 1 刀只在原位包一层 Ops；第 2 刀动手前先列出哪些函数能拆。

### 3.2 现有 `TASK` 结构

定义在 `Include/Scheduler.h`。与调度相关字段：

- `State`：`UNUSED` / `READY` / `RUNNING` / `BLOCKED` / `ZOMBIE`
- `Priority`、`Affinity` / `HomeCpu` / `OnCpu`、`InRunQueue`
- `Ticks`：计数，**不是**时间片
- **无** `next`/`prev`：就绪队列是每核 `TASK *Slot[]` 数组

`Process.h` 是 exec/brk 等 API，**不是**进程实体；可调度实体就是 `TASK`。

政策代码只应碰 State / Priority / 亲和 / InRunQueue。学生模块**不宜**直接 `#include SchedulerPrivate.h`（含 `gTasks`、锁）；应提供更薄的 Policy 辅助，或把锁留在框架侧。

### 3.3 现有 Makefile / 构建

- 真构建在 `Makefile`；`build.sh` 为包装。
- 源文件靠 **wildcard**：`CORE_SRCS += $(wildcard Core/Scheduler/*.c)`。
- 已有旋钮：`ARCH` / `BOARD` / `LWIP` / `TOY_DEMO_DRIVER`——**没有** `SCHEDULER=`。
- **没有** `Common/Modules/`、`Tests/`、`runtests`。

**推论**：学生 `.c` **不能**放进 `Core/Scheduler/`（会被默认链进 Kernel）。须用 `SCHEDULER=` **显式**拉取 `Common/Modules/...`。

### 3.4 现有可替换先例

| 模式 | 位置 | 选择方式 |
| ---- | ---- | -------- |
| `FS_OPS` | `Include/Vfs.h` | `VfsRegister` / `VfsSelect` |
| `BLOCK_BACKEND` | `Include/Block.h` | `BlockRegisterBackend` / Attach |
| `NET_BACKEND` / `INPUT_BACKEND` | Driver* | `ToyDriver*Attach` |
| `WINDOW_OPS` 等 | `Include/CoreOps.h` | Register |

调度器阶段 1 采用：**编译期选实现 + `SchedulerOpsRegister`**（形似 FS_OPS，但默认由 Make 决定链哪个 `.c`）。

### 3.5 打算如何抽接口

相对「框架放 `Common/Library/Scheduler.c`」的微调（贴现有分层）：

```
Include/SchedulerOps.h              # 新：SCHEDULER_OPS
Include/Scheduler.h                 # 对外 API 不动
Core/Scheduler/*.c                  # 框架保留（Create/Timer/Wait…）
Core/Scheduler/SchedulerOps.c       # 薄胶水：Register + 默认转发（≤50 行）
Common/Modules/SchedulerRoundRobin/SchedulerRoundRobin.c
                                    # 现 SchedulerRunq.c 政策迁入
```

**不建议**把框架整体搬到 `Common/Library/`（会与 Core/Syscall/Process 分层打架）。

`SCHEDULER_OPS` 最小集合：

```c
typedef struct {
    void   (*Init)(void);
    void   (*Enqueue)(UINT32 Cpu, TASK *T);
    void   (*Remove)(TASK *T);
    UINT32 (*PickHome)(const TASK *T);
    TASK  *(*PickNext)(UINT32 Cpu);
    /* 可选后续：int (*ShouldPreempt)(TASK *Cur); */
} SCHEDULER_OPS;

void SchedulerOpsRegister(const SCHEDULER_OPS *Ops);
const SCHEDULER_OPS *SchedulerOpsGet(void);
const SCHEDULER_OPS *SchedulerRoundRobinOps(void);
```

落地要点：

1. `SchedulerInitialize` 内 `SchedulerOpsRegister(SchedulerRoundRobinOps())`。
2. `OnTimer` / `Yield` 调 `Ops->PickNext`，不再直调本地 `PickNext`。
3. Makefile：`SCHEDULER ?= round-robin` → 只链对应 Modules 目录；**不** wildcard 扫学生树。
4. 默认行为 = 今日入队序（高优先级靠前，同级 FIFO）。steal 与 idle 兜底留在框架 `PickNext` 里，v1 不进 Ops。
5. 锁优先留在框架；模块只做排序/挑选，避免学生死锁。

### 3.6 打算如何实现测试

现状：**零测试骨架**，需新建。

| 层 | 做什么 |
| -- | ------ |
| **Host 单测（首选）** | `Tests/TestScheduler.c` + stub（锁 / `HalCpuCount` / idle），只链所选 `SCHEDULER` 模块；测入队次序、同优 FIFO、亲和、空队列→idle |
| **`runtests scheduler`** | 新 `Scripts/runtests.sh` 或 Make 目标，链 `Build/Tests/TestScheduler`，失败非 0 |
| **默认 Kernel** | 永不链学生树；仅 `SCHEDULER=student`（或指定目录）时链入 |
| **QEMU smoke** | 回归用，不替代政策评分 |

验收：

| 命令 | 期望 |
| ---- | ---- |
| `make SCHEDULER=round-robin`（或默认） | 与现网行为一致 |
| `runtests scheduler` | 对 RR Ops 断言通过 |

### 3.7 阶段 1 的 4 刀

一次只做一刀。第 1 刀确认后再写代码。

| 刀 | 交付 | 本刀不动 | 验收 |
| -- | ---- | -------- | ---- |
| **1 Ops 胶水** | `Include/SchedulerOps.h`、`Core/Scheduler/SchedulerOps.c`。`SchedulerInitialize` 注册 `SchedulerRoundRobinOps()`。`Enqueue`/`Remove`/`PickHome`/`PickNext` 经 `gOps` 调，**实现仍在 `SchedulerRunq.c`** | 不迁文件；不改 steal / idle / `Ticks` | 三架构 build；`smoke-boot.sh` + `smoke-virt.sh`；`ps` / 桌面 / fork 与现网一致 |
| **2 迁政策** | 纯政策进 `Common/Modules/SchedulerRoundRobin/`。Makefile `SCHEDULER ?= round-robin`，**显式**列文件，不用 wildcard 扫 `Modules/` 或 `Student/` | 锁、steal、`gRunQueue`、`gIdleSlot` 留 `SchedulerRunq.c` | 默认 `make` + smoke 与第 1 刀一致；`make SCHEDULER=nosuch` 报错 |
| **3 Host 单测** | `Tests/Stub/` + `Tests/TestScheduler.c` + `runtests scheduler`。断言：高优先先出、同优 FIFO、亲和、空队列、Remove、PickHome | 默认 Kernel 不链 `Student/` | `runtests scheduler` 对 RR 通过 |
| **4 学生模板** | `Student/SchedulerPriority/` + `Documents/开发/如何写一个调度器.md` | 学生树不进默认 Kernel | 模板能编；`SCHEDULER=priority` 能跑；`runtests` 对学生模板也过 |

第 2 刀动手前必须先交 `SchedulerRunq.c` 的函数分类（政策 / 框架 / 混在一起），确认后再迁。

### 3.8 风险

| 风险 | 对策 |
| ---- | ---- |
| wildcard 误链学生代码 | `SCHEDULER=` **显式**列表；禁扫 `Modules/` 与 `Student/` |
| SMP / 锁 | 锁留框架；Ops 的 Enqueue/Remove 假定已持锁 |
| idle / steal / `Ticks` | v1 不进 Ops。idle 经 `gIdleSlot` + `IdleTaskForCpu` |
| `PickNext` 混装 | 第 1 刀原位封装；第 2 刀先分类再拆 |

---

## 四、阶段 2 · 内存分配器（摘要）

与阶段 1 同模式：抽出 `MemoryOps`（或 Physical/Heap 政策面），默认实现迁 `Common/Modules/`，`MEMORY=` 选择，独立 `Tests/TestMemory.c`。细则在阶段 1 收官后补写。

---

## 五、阶段 3 · 文件系统（摘要）

`FS_OPS` / `VfsRegister` **已存在**。本阶段重点：编译期可选后端目录、学生 FAT/简易 FS 不进默认镜像、统一 `runtests fs`。细则在阶段 2 后补写。

---

## 六、Agent / 后人纪律

1. **先确认再写代码**；一次只做一个阶段。
2. 默认实现行为不变；对外 `Scheduler*` 等签名不变。
3. 新 `.c` ≤300 行；学生树不进默认 Kernel。
4. 收官后：本规划可保留为活文档，细表追加路线图 §七 `【归档】可替换模块化`。

---

## 七、修订记录

| 日期 | 说明 |
| ---- | ---- |
| 2026-09-22 | 初稿：阶段 1 调度器分析入库；代码未动 |
| 2026-09-24 | 阶段 1 拆成 4 刀。steal / idle / `Ticks` 留框架。第 1 刀只加 Ops 胶水，不搬 `SchedulerRunq.c` |
