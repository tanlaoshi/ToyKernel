# Store/ — 本地商店缓存（PR-S0 占位）

Guest 卷根：`Store/`。

- **S0**：仅约定目录；可空。  
- **S1**：优盘/第二卷拷入的包可暂存在此再安装到 `Apps/`。  
- **S2**：联网下载落盘缓存。

与只读目录源 `Assets/Store/` 区分：那边是镜像预置 catalog；这边是可写缓存。详见 `Assets/Store/README.md`。
