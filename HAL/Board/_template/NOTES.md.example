# Board 笔记模板（可选）

复制为 `NOTES.md` 后记录真机踩坑。不要把秘密或专有固件 blob 提交进库。

## 硬件

- 板修订 / 批次：
- UART 接头位置 / 电平：
- 启动介质（SD / eMMC / 网）：

## 厂商 U-Boot

- 获取镜像的 URL 或包名：
- 默认环境变量（`kernel_addr_r` / `fdt_addr_r` 等）：
- 曾试过但失败的命令：

## DRAM / DTB

- `/memory` 是否可信：
- 若无 DTB，板级表写在哪：

## 验收日志摘录

```text
（粘贴串口 ToyOS ready 前后若干行）
```
