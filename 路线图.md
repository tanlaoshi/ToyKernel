# ToyOS 发展路线图

> **排版约定（累加模式）**  
> 1. **未实现 / 进行中** 永远放在文档**前半**（按推荐实现顺序）。  
> 2. 某项验收通过后：从前半划掉或删除，**整节搬到文末「已实现归档」**，按完成时间追加。  
> 3. 文末只累加、不回头改顺序；阅读时「从上往下 = 还要做什么」，「从归档往下 = 已经做过什么」。

更细的文件级阅读顺序见 [`结构说明.md`](结构说明.md) 第 10 节；分层与 API 边界见 [`架构分层.md`](架构分层.md)。

---

# 一、未实现 / 进行中

## 1.1 下一步总目标（产品向）

| # | 目标 | 说明 |
|---|------|------|
| 0 | ~~系统分层说明~~ | ✅ 见 [`架构分层.md`](架构分层.md)（PR-L0） |
| 1 | **完善文件系统** | 在现有 Block/GPT/FAT 上补强（大文件、健壮性；多卷 ✅ FS2） |
| 2 | ~~轻量设置库~~ | ✅ `TOYOS.DB` KV（PR-DB1）；仍写 `THEME.CFG` 供 Boot |
| 3 | ~~文件浏览器~~ | ✅ FB1 只读+打开；FB2 删除/新建/重命名 |
| 4 | ~~开发接口 / C 运行时~~ | ✅ CRT1 最小集；CRT2 文件 API/`errno` + CAT/WRITE |
| 5 | ~~多语言~~ | ✅ I18N1 字形/UTF-8；I18N2 UI 字符串表 + `lang` 持久化 |
| 6 | **架构解耦续** | 门面已完成（PR-1～6）；A1～A2 ✅；**PR-A6** virt 串口 hello ✅；续 A3～A5 → **PR-A7** 最小可链接完整 Common；真 Boot 仍 1.3 |

## 1.2 执行计划（按 PR，推荐顺序）

> 当前遗留：QEMU 改分辨率须退出重跑；SMP 在宿主极忙时 AP 仍可能超时（已 `--kill-qemu` / 单核降级 / Guest 单核继续）。  
> 状态：⬜ 未开始 · 🔧 进行中 · ✅ 完成后请整行迁入文末归档

| 序 | PR | 名称 | 目标 | 范围 / 验收要点 | 依赖 | 状态 |
|----|-----|------|------|-----------------|------|------|
| — | （产品向 PR 暂无；下一刀见 **1.2b**） | | | | | |

**建议节奏**：架构解耦 **1.2b**（A6 ✅；下一刀 `PR-A3`/`PR-A4` → `PR-A5` → `PR-A7`）。

## 1.2b 架构解耦（下一阶段）

> **PR-1～6 已完成 HAL 门面**（Common 经 `Hal*` 访问硬件；Serial/ATA 等驱动留在 `HAL/X86_64/Drivers/`，**不要再搬文件位置**）。  
> 剩下的耦合在 Common 的 **CPU 上下文 / syscall / 页表软件语义 / ELF 机器号与重定位**；Arm64/RiscV 仍为 stub（当前 `make ARCH=arm64` / `ARCH=riscv` **无法链接出完整内核**）。  
> **QEMU 可测机型：`virt`。** 第一刀不是 UEFI + ToyBoot，而是 `-kernel` + 串口 MMIO hello（**PR-A6**；A1/A2 门面已够，**不必等 A3～A5**）。第一调试通道是串口不是 VGA（与 [`结构说明.md`](结构说明.md) §2 启动顺序「串口最先」一致）。  
> **宿主机环境**（PR-A6 前置）：系统交叉编译器/`qemu-system-aarch64|riscv64`，或见 `tools/README.md`（本机可用 xPack + 解压的 QEMU deb）。可选 AAVMF。RISC-V UEFI 需 QEMU ≥8.1，本机 6.2 **先不做 UEFI**。
> NVMe/AHCI/virtio-blk、ToyBoot 移植、GOP/Video、真 SMP 已列 **1.3 后置**（结构说明 I1），本阶段不单开 PR。

| 序 | PR | 名称 | 目标 | 范围 / 验收要点 | 依赖 | 状态 |
|----|-----|------|------|-----------------|------|------|
| 1 | **PR-A3** | **页表软件语义进 HAL** | 6 | `HalPageMarkCow` / `HalPageIsCow` 或 `HalPagePrepareUserRoot`；Common 不再直接写 `PTE_COW`（bit9）、PML4[0]、硬编码槽 0 | PR-A1 | ⬜ |
| 2 | **PR-A4** | **ELF 按 arch** | 6 | `HalElfMachine()` / 重定位表放 HAL，或 ARCH 选 `ElfReloc*.c`；`Common/Library/Elf.c` 不再写死 `EM_X86_64` / `R_X86_64_*` | PR-A1 | ⬜ |
| 3 | **PR-A5** | **清掉 Common 内联 asm** | 6 | `HalIrqSave`/`Restore`、`HalCpuRelax()` 取代 Gui.c `pushfq` 与 SpinLock/`Scheduler` 的 `pause`；A1～A2 之后 Common 不再 `#include HalPort.h` 漏出 `Arch*` | PR-A1、PR-A2 | ⬜ |
| 4 | **PR-A7** | **Arm64/RiscV 最小可链接完整 Common** | 6 | 各 arch：最小页表 + IRQ/timer；能链进更多 Common（对齐原「A6 可链接」）。`make ARCH=arm64` / `ARCH=riscv` 能链接完整 `KernelMain` 路径，而非仅 hello。真 Boot / `HalSmp*` 仍见 1.3 后置 | PR-A3～A5、PR-A6 ✅ | ⬜ |

**建议节奏**：（`PR-A3` ∥ `PR-A4` → `PR-A5`）→ `PR-A7`。A6 ✅；A7 等 A3～A5。

## 1.3 其它未排期缺口（穿插 / 后置）

| 优先级 | 项 | 说明 |
|--------|-----|------|
| 可穿插 | `execve` / 管道或简单信号 | 教学向进程扩展（择一） |
| 可穿插 | 默认 `LWIP=1` 或文档强化 | 生产路径更明显 |
| 可穿插 | DNS / 更完整 socket 错误码 | 按需 |
| 可穿插 | 减弱调度大锁 / 优先级 | SMP 可选 |
| 可穿插 | `shell`/`gui` 可迁移到 AP | 需输入同步；现钉 BSP |
| 可穿插 | `GuiTask` 分工 / `UI.c` 接入 | G10 L1 全拆分、控件库 |
| 可穿插 | 运行中热切换分辨率 | virtio-gpu 等；不替代 D7 |
| 后置 | NVMe/AHCI / virtio-blk / 安装器 | 真机读写盘；第二 Block 后端（结构说明 I1）。**不与 A 系列重复**；A 系列后再穿插 |
| 后置 | 真机 IOAPIC / 中断路由 | SMP |
| 后置 | Arm64 / RiscV 真 Boot 与 `HalSmp*` | 现为 stub。**先 `-kernel` 串口 hello（PR-A6）**，再 PR-A7 最小可链接；真 Boot（移植 ToyBoot `BOOTAA64.EFI`、AAVMF / RiscVVirt UEFI）、GOP/Video、virtio 块、`HalSmp*` 仍后置。本机 QEMU 6.2，RISC-V UEFI 需 ≥8.1，本阶段不做 UEFI |
| 后置 | 完整 POSIX / 用户堆 `brk`/`mmap` / swap | — |

## 1.4 模块缺口速览

| 模块 | 仍缺 |
|------|------|
| 进程/系统调用 | 任务槽约 8；无 `execve`/`mmap`/`brk`/信号/管道；FD 整文件缓冲；`.so` 仅 JUMP_SLOT |
| 存储 | ATA PIO；写 ≤1MB；多卷前缀 ✅（PR-FS2）；无 NVMe/安装器（1.3 后置，不单开 A 系列） |
| GUI | `UI.c` 未全面接入；I18N1 ✅ UTF-8/CJK 子集；无 TTF/完整 Unicode |
| 网络 | builtin TCP legacy；lwIP 无 DNS；用户 socket 需 `LWIP=1` |
| SMP | shell/gui 钉 BSP；极忙宿主仍可能 AP 超时（已 kill/单核工具） |
| 跨架构 | A1～A2、**A6** ✅（`virt` `-kernel` 串口 hello）；RiscV 需 `-bios none`；**PR-A7** 最小可链接完整 Common；真 Boot/`HalSmp*` 仍 1.3 |

---

# 二、已实现归档（按完成时间累加，只追加）

> 下列阶段已验收。新完成项请追加在本节**最末**，不要插到中间打乱历史顺序。

---

## 【归档】阶段 1：虚拟内存 + 系统调用（Ring 3）✅

| 组件 | 文件 | 内容 |
|------|------|------|
| 分页 | `Common/Core/VirtualMemory.c` + `HAL/.../Page.c` | 恒等映射、MMIO、用户页 |
| GDT 用户段 | `Arch.c` | 0x23 / 0x1B、TSS |
| 系统调用 | `Syscall.c/h` | 分发共用；`int 0x80` legacy + `syscall`/`sysret` |
| 用户演示 | `User/hello.S` | Ring 3 打印后 exit |

**验证**：`runuser` → `Hello Ring3!`

---

## 【归档】阶段 2：进程隔离 ✅

| 组件 | 文件 | 内容 |
|------|------|------|
| 独立页表 | `VirtualMemory.c` | `VM_ADDR_SPACE`、`SpaceClone` |
| ELF 加载 | `Elf.c`、`Process.c` | 静态 `ET_EXEC`；`DT_NEEDED` + `JUMP_SLOT` 简易 `.so` |
| fork/wait/yield | `Scheduler.c`、`Syscall.c` | **COW**；`wait` 可 `WNOHANG` |
| open/read/write/close | `Syscall.c` + 每任务 FD | 打开读入缓冲；close 刷盘 |
| 用户程序 | `hello`/`count`/`fork`/`waitnh`/`dyndemo` | + `LIBTOY.SO` |

**验证**：

```
toyos> exec FORK.ELF / SYSHELLO.ELF / SYSFORK.ELF / WAITNH.ELF
toyos> exec DYNDEMO.ELF   # 需 LIBTOY.SO
toyos> exec CAT.ELF / WRITE.ELF
toyos> ps
```

- **COW fork**：共享用户页 + `PTE_COW`；`#PF` 拆分；`PhysicalMemoryRetain/Release`
- **WNOHANG**：`SYS_WAIT` 的 `rdi`；有子进程无僵尸时返回 `0`
- **动态库**：`DT_NEEDED` → `USER_SO_BASE`；`R_X86_64_JUMP_SLOT`
- **PR-SC1**：`syscall`/`sysret` 与 `int 0x80` 解耦

---

## 【归档】存储旁路 ✅ 基础

| 组件 | 文件 | 内容 |
|------|------|------|
| Block / ATA | `Block.c`、`Ata.c` | 扇区读写；PIO |
| GPT / FAT | `Gpt.c`、`Fat.c` | superfloppy/MBR/GPT；8.3+**LFN**；`DIR/FILE`；`.`/`..`；`mkdir`/`rmdir` |
| FS 组装 | `FileSystem.c` | 优先 `TOYOS.ID`；`ls`/`cat`/`write`/`rm`/`mkdir`/`rmdir` |
| ToyBoot / QEMU | `Boot.c`、`run-split.sh` | 跨卷 Kernel；双盘 ESP+`rootfs/` |

**验证**：`write`/`cat`/`ls`；长文件名 LFN；`ls DOCS` / `rm` 空目录。

---

## 【归档】阶段 3：图形界面 G1～G5 ✅

| PR | 主题 | 状态 |
|----|------|------|
| PR-G1 | 标题栏关闭钮、焦点色 | ✅ |
| PR-G2 | 每窗独立终端 + 焦点 save/restore | ✅ |
| PR-G3 | clear/滚动/仅焦点窗键盘 | ✅ |
| PR-G4 | 拖动重叠：整窗备份合成 | ✅ |
| PR-G5 | 拖动脏区离屏写出 | ✅ |

窗口 / 鼠标 / 视频：`Gui.c`、`XHCI`+tablet、`HalVideo`；`UI.c` 控件库已有、GUI 未全面调用。

---

## 【归档】阶段 3b：桌面 + 设置 + 字体 + 合成 G6～G10 ✅

**原则**：加字体 = `Fonts/` 注册；Settings/Gui 不直接 include 点阵表。

| PR | 主题 | 状态 |
|----|------|------|
| PR-D1 | 字体子系统 `FONT_FACE` | ✅ |
| PR-D2 | Theme 桌面/Shell/字体 | ✅ |
| PR-D3 | 窗口种类 + `GuiOpenShell/Settings` | ✅ |
| PR-D4 | 桌面图标双击 | ✅ |
| PR-D5 | Settings 字体/背景 | ✅ |
| PR-D6 | `THEME.CFG` 持久化 | ✅ |
| PR-D7 | Settings `mode=WxH` → ToyBoot GOP（重启/重跑脚本生效） | ✅ |
| PR-G6 | 合成露底正确（窗备份） | ✅ |
| PR-G7 | 短 cli + `GfxIrq` | ✅ |
| PR-G8 | Theme 单次 Compose | ✅ |
| PR-G9 | Backbuffer Present | ✅ |
| PR-G10 | clip/鼠标/双击 tick/滚屏 backup 等 | ✅ |

**分辨率（D7）**：推荐「写盘 → 退出 QEMU → `./run-split.sh`」。Guest `reboot` 不改宿主 edid；VM 上 ToyBoot 跳过会触发 GTK 环的 `SetMode`。颜色/字体可用 Guest reboot。

**IRQ 原则（G6～G10）**：合成尽量 `sti`；只锁光标与单次 Present。

**本阶段明确不做（仍属未实现）**：图标拖放、开始菜单、壁纸位图、TTF、多语言、用户态 GUI toolkit、运行中热切换分辨率。

---

## 【归档】阶段 4：网络栈 ✅ 基本完成

| 组件 | 内容 |
|------|------|
| virtio-net | ARP、ICMP、`NetSendIp` |
| 自研 UDP/TCP | 发送缓冲 + 窗口 + 重传 + echo（legacy） |
| lwIP | `make LWIP=1`；ping/echo/UDP/connect；RX 统一 |
| PR-N1～N8 | 脚手架 → socket 用户态（`NETDEMO`/`NETSRV`） |

**Builtin 验证**：`ping` / `tcplisten 9000` / `tcpstatus`  
**lwIP 验证**：`lwip on`；`tcpconnect`；`udpsend`/`udplisten`；`exec NETDEMO.ELF` / `NETSRV.ELF`  
**Socket ABI**：`socket` 8 … `accept` 12；见 `Include/Socket.h`。

`run-split.sh` 已 `hostfwd=tcp::9000-:9000`。

---

## 【归档】阶段 5：多核 SMP ✅ S1～S4

**原则**：Common 一套调度；MADT/SIPI/LAPIC 等在 `HAL/<Arch>/`。

| PR | 主题 | 状态 |
|----|------|------|
| PR-S1 | MADT + INIT/SIPI；AP `smp: hello` | ✅ |
| PR-S2 | 每核 timer + `HalCpuId`；AP tick 不调度 | ✅ |
| PR-S3 | 自旋锁；调度大锁；AP idle | ✅ |
| PR-S4 | 每核 TSS + READY 队列 + 偷任务 | ✅ |

**验证**：`./run-split.sh`（`-smp 2`）；串口 `hello` / `ticks`；`ps` 见 `idle0`/`idle1`；`exec FORK.ELF` 不崩。

AP 超时后 park + 单核继续（稳健性收尾见归档 **PR-Q1**）。

---

## 【归档】架构重构 PR-1～6 ✅

| PR | 内容 |
|----|------|
| PR-1 | `Common/{Core,Services,Library}`、`HAL/<Arch>/Drivers/` |
| PR-2 | API → `Include/`；`Hal.h` 门面 |
| PR-3 | `BOOT_INFO` + `KernelMain`；UEFI 细节仅 Startup |
| PR-4 | Block/USB/Net HAL 门面；`KernelModules`/`ShellCommands`/`Tasks` |
| PR-5 | `HalSerial` / `HalVideo`；Common 不 include 驱动头 |
| PR-6 | HAL 去 x86 对外命名（`HalIrqVectorSet`、`PageRoot` 等） |

**验证**：`./build.sh`；`exec FORK.ELF`；`rg 'Drivers/' Common` 无匹配。

---

## 【归档】里程碑清单（速查）

- ~~PR-L0 架构分层说明书~~ → [`架构分层.md`](架构分层.md) ✅
- ~~PR-FS1 FAT 语义补强~~ ✅  
- ~~PR-FS2 多卷 / 路径前缀~~ ✅  
- ~~PR-Q1 QEMU/SMP 启动稳健性~~ ✅  
- ~~PR-1～6 架构~~ ✅  
- ~~阶段 1～2 + SC1 + COW + WNOHANG + .so + LFN~~ ✅  
- ~~存储 Block/FAT + 双盘 Boot~~ ✅  
- ~~PR-G1～G10、PR-D1～D7~~ ✅  
- ~~PR-N1～N8~~ ✅  
- ~~PR-S1～S4~~ ✅  

---

## 【归档】PR-L0：分层与接口说明书 ✅

- 文档：[`架构分层.md`](架构分层.md)
- 覆盖：Boot → HAL → Fonts → Library → Core → Services → User
- 内容：每层公开 API、禁止跨层规则、syscall ABI、与 `结构说明.md` 分工

---

## 【归档】PR-FS1：FAT 语义补强 ✅

- `FatMkdir` / `FatRmdir`；Shell `mkdir` / `rmdir`
- 路径组件 `.` / `..`（解析栈）
- 统一 `FAT_OK` / 负错误码 + `FatStrError`（调用方按码判断）
- 多分簇写：目录簇可扩展；写上限 1MB；失败回滚簇链
- 空目录仍可用 `rm` 删除（与 `rmdir` 并存）

**验证**：

```
toyos> mkdir DOCS
toyos> ls DOCS
toyos> write DOCS/A.TXT hello
toyos> cat DOCS/../DOCS/A.TXT
toyos> rm DOCS/A.TXT
toyos> rmdir DOCS
```

---

## 【归档】PR-Q1：QEMU/SMP 启动稳健性 ✅

- 文档：[`ToyImage/QUICK_START.md`](../ToyImage/QUICK_START.md)；`run-split.sh --help`
- `--kill-qemu` / `TOY_KILL_QEMU=1`：启动前 `pkill` 残留实例
- 残留 QEMU 时默认 `TOY_SMP=1`；`--smp=N` / `--headless`
- Guest：AP timeout → park + `smp: continue single-CPU`（已有）
- 冒烟：`ToyImage/smoke-boot.sh`（默认 `TOY_SMP=1`）
- CI：ToyKernel `build.yml`；ToyImage `smoke.yml`（`TOY_SMP=1`）

**验证**：`./smoke-boot.sh` 串口出现 `ToyOS ready`。

---

## 【归档】PR-FS2：多卷 / 路径前缀 ✅

- 扫描全部 Block 盘挂载 FAT；`vols` 列出卷（盘符 `A:`/`B:` + 名）
- 路径前缀：`TOYOS:` / `ESP:` / `A:` / `0:`；无前缀用默认卷（优先含 `TOYOS.ID`）
- `FsRead/Write/List/…` 供 Shell、`exec`、open、Theme；写拒只读卷（`FAT_ERR_ROFS`）
- GPT EFI System → 只读；有 TOYOS 时其余卷亦只读并名 `ESP`
- `GptFindFatStartEx`；FD 路径扩至 64

**验证**：

```
toyos> vols
toyos> ls
toyos> ls TOYOS:
toyos> ls A:
toyos> exec TOYOS:HELLO.ELF
toyos> write ESP:X.TXT no   # 期望 read-only volume
```

---

## 【归档】PR-DB1：轻量 KV（TOYOS.DB）✅

- `Db.c/h`：卷上 `TOYOS.DB`（`key=value` 文本）；`DbGet`/`DbSet`/`DbDelete`/`DbForEach`
- Shell：`dbget` / `dbset` / `dblist`
- 无库时自 `THEME.CFG` 导入；`ThemeLoad` 优先 DB，`ThemeSave` 写 DB + 仍写 `THEME.CFG`（Boot `mode=`）
- `DbInit` 在 `InitGuiModule`、`ThemeLoad` 之前

**验证**：

```
toyos> dblist
toyos> dbget desktop
toyos> dbset demo hello
toyos> dbget demo
toyos> cat TOYOS.DB
# Settings 改色/字体后应同时更新 TOYOS.DB 与 THEME.CFG
```

---

## 【归档】PR-FB1：文件浏览器（只读+打开）✅

- `FatListEntries` / `FsListEntries`：结构化目录枚举
- `FilesUi` + `GUI_WIN_FILES`；桌面 **Files** 图标；Shell `files`
- 进目录；双击/Enter：`.ELF` → `ProcessExec`；其它 → 文本预览；Esc 返回
- 无删除/新建（PR-FB2）

**验证**：

```
toyos> files
# 或桌面双击 Files
# 列表选 HELLO.ELF Enter；选 THEME.CFG 预览；Esc 返回
```

---

## 【归档】PR-FB2：文件浏览器写操作 ✅

- 删除：`d` / Delete → Y/N 确认；空目录 `FsRmdir`，文件 `FsDeleteFile`
- 新建：`n` mkdir；`f` 空文件（`FsWriteFile` size 0）
- 重命名：`r` + 名称；`FatRename`/`FsRename`（同卷改目录项）；Shell `mv`
- 确认/输入框叠在列表上；Esc 取消

**验证**：

```
toyos> files
# n → DEMO → Enter；进 DEMO；f → NOTE.TXT；r 改名；d + Y 删除
toyos> mkdir T
toyos> mv T U
toyos> rmdir U
```

---

## 【归档】PR-CRT1：用户态 CRT 最小集 ✅

- `User/include/`：`toy_syscall.h`、`string.h`、`stdio.h`、`stdlib.h`
- `User/crt/`：`crt0.S`、`syscall.S`（`syscall` 指令）、`string.c`、`printf.c`（`%s%d%u%x%c`）、`malloc.c`（BSS 8KiB bump，无 brk）
- `user.ld` 增加 `.data`/`.bss`；`HELLO.ELF` 改为 `hello.c` + CRT（旧 `int 0x80` 见 `hello_int80.S`）

**验证**：

```
toyos> exec HELLO.ELF
# 期望输出含 Hello Ring3
```

---

## 【归档】PR-CRT2：CRT 文件 API + errno ✅

- `errno.h` / `fcntl.h` / `unistd.h`；`crt/errno.c`、`crt/unistd.c`（`open`/`read`/`write`/`close` 失败置 `errno`）
- `perror`；`SYS_OPEN` 路径上限与 `TASK_FD.Path` 对齐（63）
- `CAT.ELF` ← `cat.c`；`WRITE.ELF` ← `writefile.c`（旧 `int 0x80` 见 `catfile.S` / `writefile.S`）

**验证**：

```
toyos> exec CAT.ELF
# 期望含 ToyOS root volume
toyos> exec WRITE.ELF
# 期望 ok；宿主 rootfs/USERNOTE.TXT 含 from ring3!
```

---

## 【归档】PR-A1：封死 Arch\* 出口 ✅

- `HalSyscallInit`（x86）：安装 `Isr128` + `ArchSyscallMsrInit`；Common `SyscallInit` 不再碰硬件
- `HalSetKernelStack` 取代 `Scheduler` 中的 `ArchSetRsp0`
- 验收：`rg 'ArchSetRsp0|ArchSyscallMsrInit' Common` 无匹配

---

## 【归档】PR-I18N1：字形与 UTF-8 汉字路径 ✅

- `Fonts/cjk16.c`：教学用 16×16 汉字子集；`Utf8Decode` / `FontGlyphCp`
- `VideoDrawString*` 按 UTF-8 码点绘制；Shell `zh` 冒烟

**验证**：

```
toyos> zh
# 期望：你好，世界！中文测试
toyos> exec FORK.ELF
# A1 回归：C / P / done
```

---

## 【归档】PR-A2：可移植任务帧 API ✅

- `HalFrameSetKernelEntry` / `HalFrameSetUserEntry` / `HalFrameCopy` / `HalFrameGetRip`
- `HalFrameSyscallNum` / `HalFrameArg0..2` / `HalFrameSetReturn` / `HalFrameSetReturn2`
- `Scheduler.c` / `Syscall.c` 不再写 `Cs`/`Ss`/`Rax`/`Rip` 或 GDT 选择子字面量

**验证**：

```
toyos> exec HELLO.ELF
toyos> exec FORK.ELF
# C / P / done
```

---

## 【归档】PR-I18N2：UI 字符串表与语言持久化 ✅

- `Locale.h` / `Locale.c`：`MSG_*` + `LocStr`；`TOYOS.DB` 键 `lang=en|zh`
- Desktop / Gui 标题 / Settings / FilesUi / Console 欢迎语走字符串表；Shell `lang en|zh`
- CJK 字形扩充；桌面标签 UTF-8 宽度；`HalVideoDrawCodepointAt`
- `smoke-boot.sh` 同时认 `ToyOS ready` / `ToyOS 就绪`

**验证**：

```
toyos> lang zh
# 语言已切换；桌面图标/窗标题中文；dblist 含 lang=zh
# 重启后串口：ToyOS 就绪…
toyos> lang en
```

---

## 【归档】PR-A6：QEMU virt 串口 hello ✅

- `BRINGUP=1`（非 x86 默认）：只链 `Startup.S`/`Startup.c` + `HalSerial` + `Hal`
- Arm64：`link.ld` @ `0x40000000`；PL011 `0x09000000`；`run-virt-arm.sh`
- RiscV：`link.ld` @ `0x80000000`；UART16550 `0x10000000`；`run-virt-riscv.sh` 使用 `-bios none`（避免与 OpenSBI 重叠）
- Makefile 自动探测 `tools/extract` xPack 或系统交叉编译器；产物 `Build/arm64|riscv/Kernel.elf`

**验证**：

```
./build.sh arm64 && ./run-virt-arm.sh
# ToyOS Arm64 virt: hello
./build.sh riscv && ./run-virt-riscv.sh
# ToyOS RiscV virt: hello
```

---

## QEMU 备忘

```bash
cd ToyKernel && ./build.sh              # 默认（builtin 网络）
cd ToyKernel && ./build.sh LWIP=1       # 含 lwIP ping/echo
cd ToyKernel && DEBUG=1 ./build.sh      # 串口调试日志
cd ../ToyBoot && ./build.sh             # 更新 BOOTX64.EFI
cd ../ToyImage && ./run-split.sh --kill-qemu   # 唯一入口；可清残留
# TOY_SMP=1 ./run-split.sh              # 宿主忙时可选单核
# ./smoke-boot.sh                       # 无头冒烟
```
