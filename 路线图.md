# ToyOS 发展路线图

五大方向及推荐实现顺序（后者依赖前者）：

```
阶段1 虚拟内存 + 用户态/系统调用  ← 已完成
阶段2 进程隔离 + ELF 加载          ← 已完成
阶段3 图形界面（鼠标 + 窗口）      ← 进行中（桌面/光标/窗口已可用）
阶段4 网络栈（virtio-net + lwIP）
阶段5 多核 SMP
```

---

## 阶段 1：虚拟内存 + 系统调用（Ring 3）✅ 已完成

| 组件 | 文件 | 内容 |
|------|------|------|
| 分页 | `VirtualMemory.c/h` | 4 级页表、恒等映射、高地址 MMIO、用户页 U 位传播 |
| GDT 用户段 | `Arch.c` | 0x23 用户代码、0x1B 用户数据、TSS（Present 位） |
| 系统调用 | `Syscall.c/h` | int 0x80 门（DPL=3），SYS_write / SYS_exit |
| 用户态演示 | `User/hello.S` | Ring 3 打印后 syscall exit |
| Shell | `runuser` 命令 | 映射用户页并跳入 Ring 3 |
| 串口输入 | `Serial.c` + Shell | 可用 `-serial stdio` 管道输入命令 |

**验证**：`runuser` → `Hello Ring3!` → `syscall: exit(0)`

---

## 阶段 2：进程隔离 ✅ 核心已完成

| 组件 | 文件 | 内容 |
|------|------|------|
| 独立页表 | `VirtualMemory.c/h` | `VM_ADDR_SPACE`、浅拷贝内核映射、CR3 切换 |
| ELF 加载 | `Elf.c/h` | 静态 `ET_EXEC`、PT_LOAD 映射 |
| 进程管理 | `Process.c/h` | `ProcessExec`、`SchedulerCreateUser` |
| 用户内存校验 | `VirtualMemory.c` | `VirtualMemoryUserAccessOk`、`VirtualMemoryCopyFromUser` |
| 用户程序 | `User/hello.S`、`User/count.S` | 两个独立 ELF 演示 |
| Shell | `exec`、`ps` | 从 FAT 加载；显示 kern/user、cr3、rip |

**验证**：
```
toyos> exec HELLO.ELF    # Hello Ring3!
toyos> exec COUNT.ELF    # 0-9 各行一个数字
toyos> ps                # 显示 user/kern、cr3、rip
```

**待做**：`fork`/`wait`、页表 refcount、多进程并发

---

---

## 阶段 3：图形界面（进行中）

| 组件 | 文件 | 内容 |
|------|------|------|
| 窗口管理 | `Gui.c/h` | 桌面、Shell 窗口、十字光标、**标题栏拖动**、点击聚焦 |
| 鼠标驱动 | `XHCI.c` | 第二 HID 设备（usb-tablet）、绝对坐标 |
| 键盘光标 | `Gui.c` + `Kernel.c` | 方向键移动光标（备用） |
| 绘图 API | `Video.c` | `VideoDrawStringAt`、`VideoCopyRect`（拖动平移） |
| QEMU | `run.sh` | `-device usb-tablet` |

**操作**：
- 按住窗口**蓝色标题栏**拖动；松开结束
- 或键盘 **方向键** 移动光标，**Enter** 模拟点击

**待做**：按钮控件、与 Shell 分离的图形终端

---

- USB 鼠标（HID 与键盘共用 XHCI 框架）
- 光标绘制、窗口矩形（复用 `UI.c`）
- 简易窗口管理器（焦点、标题栏）
- 可选：与 Shell 分离的 GUI 任务

---

## 阶段 4：网络栈（已启动）

| 组件 | 文件 | 内容 |
|------|------|------|
| QEMU | `run.sh` | `virtio-net` + user；`hostfwd` UDP 5555 / TCP 2222→7 |
| 驱动 | `Net.c/h` | virtio-net 现代 PCI、RX/TX、ARP、ICMP、`NetSendIp` |
| UDP | `Udp.c/h` | bind / send / 接收队列 |
| TCP | `Tcp.c/h` | 单连接 listen 回显 / connect（无重传） |
| Shell | `net` `ping` `udplisten` `udpsend` `tcplisten` `tcpstatus` | |

默认 IP：`10.0.2.15/24`（QEMU user 网络）

**验证**：
```
toyos> ping 10.0.2.2
reply from 10.0.2.2
toyos> udplisten 5555
# 宿主机: echo hello | nc -u 127.0.0.1 5555
toyos> tcplisten 7
# 宿主机: nc 127.0.0.1 2222   # hostfwd → guest:7
```

**已修**：virtio cfg 偏移、12 字节 net hdr、校验和字节序、RX kick。

**待做**：TCP 重传/窗口；多缓冲 TX；或嵌入 lwIP

---

## 阶段 5：多核 SMP

- ACPI MADT / MP 表解析
- AP 启动（SIPI + 跳板）
- 每核 LAPIC 定时器
- 自旋锁保护 PMM / 调度器
- 负载均衡（可选）

---

## QEMU 参数备忘

```bash
# 调试日志开关（默认关闭）
./build.sh              # 内核 TOY_DEBUG=0
./build.sh DEBUG=1      # 打开 [mod]/PMM/XHCI/net 等串口调试
cd ../ToyBoot && ./build.sh          # Boot 调试关
cd ../ToyBoot && ./build.sh DEBUG=1  # BootDbg 开
```

```bash
# 阶段4 网络（含 hostfwd 便于宿主机测 UDP/TCP）
-device virtio-net-pci,netdev=n0 \
-netdev user,id=n0,hostfwd=udp::5555-:5555,hostfwd=tcp::2222-:7

# 阶段3 鼠标（已有 xhci + usb-kbd，追加）
-device usb-tablet,bus=xhci.0

# 阶段5 SMP
-smp 2
```
