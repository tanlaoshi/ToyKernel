# ToyOS 发展路线图

五大方向及推荐实现顺序（后者依赖前者）：

```
阶段1 虚拟内存 + 用户态/系统调用  ← 已完成（含 PR-SC1 syscall/sysret）
阶段2 进程隔离 + ELF / fork / 系统调用扩展  ← 核心完成
阶段3 图形界面（鼠标 + 窗口）      ← 基本可用（G1～G5 ✅）
阶段3b 桌面 + 设置 + 字体解耦     ← 下一步主线（PR-D1～D5）
阶段4 网络栈（virtio-net + lwIP）   ← 基本完成（PR-N8 用户态 socket 已通）
阶段5 多核 SMP                     ← 基本完成（PR-S1～S4 ✅）

旁路加强（可穿插）：
  存储 Block + FAT 读写 + 双盘 Boot  ← 已完成基础
  架构重构 PR-1～4（目录/Boot/HAL）  ← 已完成（已合并 main）
  架构重构 PR-5（HalVideo/HalSerial） ← 已完成
  架构重构 PR-6（HAL 去 x86 命名）   ← 已完成
  真机 NVMe/AHCI / 双系统安装器     ← 后置
```

更细的**文件级实现/阅读顺序**见 [`结构说明.md`](结构说明.md) 第 10 节。

---

## 阶段 1：虚拟内存 + 系统调用（Ring 3）✅

| 组件 | 文件 | 内容 |
|------|------|------|
| 分页 | `Common/Core/VirtualMemory.c` + `HAL/.../Page.c` | 恒等映射、MMIO、用户页 |
| GDT 用户段 | `Arch.c` | 0x23 / 0x1B、TSS |
| 系统调用 | `Syscall.c/h` | 分发共用；`int 0x80` legacy + `syscall`/`sysret` |
| 用户演示 | `User/hello.S` | Ring 3 打印后 exit |

**验证**：`runuser` → `Hello Ring3!`

---

## 阶段 2：进程隔离 ✅ 核心完成

| 组件 | 文件 | 内容 |
|------|------|------|
| 独立页表 | `VirtualMemory.c` | `VM_ADDR_SPACE`、`SpaceClone` |
| ELF 加载 | `Elf.c`、`Process.c` | 静态 `ET_EXEC`；`DT_NEEDED` + `JUMP_SLOT` 简易 `.so` |
| fork/wait/yield | `Scheduler.c`、`Syscall.c` | **COW** 地址空间；`wait` 可 `WNOHANG` |
| open/read/close | `Syscall.c` + 每任务 FD | 打开时读入缓冲；close 写回 FAT |
| open/write/close | `Syscall.c` + `Scheduler.c` | `SYS_WRITE` 写 FD；close 刷盘 |
| 用户程序 | `hello`/`count`/`fork`/`waitnh`/`dyndemo` | + `LIBTOY.SO` |

**验证**：
```
toyos> exec FORK.ELF     # P / C / done
toyos> exec SYSHELLO.ELF # Hello SYSCALL!（syscall/sysret）
toyos> exec SYSFORK.ELF  # 同上 fork，走 syscall
toyos> exec WAITNH.ELF   # P / C / done（父用 WNOHANG+yield 轮询）
toyos> exec DYNDEMO.ELF  # from.so / done（需同卷有 LIBTOY.SO）
toyos> exec CAT.ELF      # 读 TOYOS.ID（需挂在含该文件的卷）
toyos> exec WRITE.ELF    # Ring3 写 USERNOTE.TXT，再 cat USERNOTE.TXT
toyos> ps
```

**待做**：（阶段 2 核心项已完成）

**COW fork（已实现）**：`VirtualMemorySpaceClone` 共享用户物理页并清 W + `PTE_COW`；`#PF` 写故障拆分；`PhysicalMemoryRetain/Release` 引用计数。验证：`exec FORK.ELF` → `P` / `C` / `done`。

**WNOHANG（已实现）**：`SYS_WAIT` 的 `rdi` 为 options；`WNOHANG(1)` 时有存活子进程但尚无僵尸则返回 `0`（不阻塞）。验证：`exec WAITNH.ELF`。

**动态库（已实现）**：`exec` 解析 `DT_NEEDED`，将 `ET_DYN` 装到 `USER_SO_BASE`，解析 `R_X86_64_JUMP_SLOT`。验证：`exec DYNDEMO.ELF`（FAT 上需有 `LIBTOY.SO`）。

---

## 存储旁路 ✅ 基础完成

| 组件 | 文件 | 内容 |
|------|------|------|
| Block | `Block.c/h` | 读/写扇区；ATA 主/从 |
| ATA | `Ata.c` | PIO 读 `0x20` / 写 `0x30` |
| GPT | `Gpt.c` | superfloppy / MBR / GPT |
| FAT | `Fat.c` | 8.3 + **LFN**；路径 `DIR/FILE`；读/写/删；`FatListDir` |
| FS 组装 | `FileSystem.c` | 优先 `TOYOS.ID` 卷；`ls`/`cat`/`write`/`rm` |
| ToyBoot | `ToyBoot/Boot.c` | 跨卷找 `Kernel.elf`；真机 EDID / QEMU 分辨率表 |
| QEMU | `run.sh` / `run-split.sh` | 单盘 / 双 FAT（ESP + rootfs） |

**验证**：
```
toyos> write NOTE.TXT hello
toyos> cat NOTE.TXT
toyos> write longfilename.txt hi   # 非 8.3 → LFN + HELLO~1 类短名
toyos> ls
toyos> cat longfilename.txt
```

**待做**：NVMe/AHCI、自动安装器

**LFN（已实现）**：目录扫描识别 `ATTR=0x0F` 长名项；`ls`/`cat`/`rm`/`write` 可用长名；写入时生成 `BASE~N.EXT` 短名别名。控制台按 Latin-1/ASCII 显示（非 BMP 显示为 `?`）。

**新增**（子目录 + 删除）：
```
toyos> ls DOCS              # 列出子目录
toyos> cat DOCS/NOTE.TXT
toyos> write DOCS/NOTE.TXT hello
toyos> rm DOCS/NOTE.TXT
toyos> rm DOCS              # 仅空目录
```

---

## 阶段 3：图形界面 ≈ 基本可用

| 组件 | 文件 | 内容 |
|------|------|------|
| 窗口 | `Gui.c` | Shell 窗、标题栏拖动、焦点切换 |
| 鼠标 | `XHCI.c` + usb-tablet | 绝对坐标；`GuiPollMouse` |
| 视频 | `Video.c` | `VideoCopyRect`、clip、局部刷新 |
| 控制台绑定 | `Console.c` + `Gui.c` | 输出限制在焦点窗客户区；`TermX/Y` 随焦点保存 |
| 控件库（未接入） | `UI.c` | `UiDrawButton` 等已有，GUI 未调用 |

**已有能力**：单 Shell 窗口、十字光标 save-under、拖标题栏移动、点击 raise 焦点、键盘进全局 Shell、方向键移光标。

**缺口 / 下一步**：见 **阶段 3b**（桌面图标、Settings、字体解耦）。可选：`GuiTask`/`ShellTask` 分工。

### GUI 收尾计划（建议 PR 顺序）

| PR | 主题 | 主要文件 | 状态 |
|----|------|----------|------|
| PR-G1 | 标题栏按钮 | `Gui.c` | ✅ 关闭钮 `[x]`、焦点蓝/非焦点灰、默认单窗 |
| PR-G2 | 独立图形终端 | `Gui.c`、`Console.c` | ✅ 每窗输入行 + 光标；切焦点 save/restore |
| PR-G3 | 体验抛光 | `Console.c`、`Gui.c` | ✅ clear 只清客户区；滚动；键盘仅焦点窗 |
| PR-G4 | 拖动重叠修复 | `Gui.c` | ✅ 整窗备份合成；残留/z 序已修复 |
| PR-G5 | 拖动闪屏优化 | `Gui.c`、`Video.c` | ✅ 脏区离屏一次写出；起止勿整屏重贴 |

**PR-G1 验证**：点标题栏红色 `[x]` 关窗（保留最后一窗）；两窗并存时非焦点标题栏为灰色。

**PR-G2 验证**：开 Shell + Shell 2；A 窗 `echo A`、B 窗 `echo B`；切窗后各自内容仍在。

**PR-G3 验证**：`clear` 不清桌面；长输出在窗内滚动；失焦窗不收键盘。

**PR-G4 验证**：两窗互拖重叠；各自 Shell 文字保留；无残留/z 序错乱（全屏合成）。

**PR-G5 验证**：两窗互拖；移动过程无明显灰闪/撕裂；松手时标题栏色正确、无整屏闪一下。

**可选（非硬性）**：`GuiTask` 专管鼠标、`ShellTask` 专管键盘；README 阶段 3 标 ✅。

---

## 阶段 3b：桌面 + 设置 + 字体解耦 ← 当前 GUI 主线

**目标形态**：启动先见桌面（可换背景色）→ 图标**双击**打开 Shell / Settings → Settings 切换字体、桌面背景与**显示分辨率**（见下）。字体资源独立目录，绘制层只经字体 API。

**原则**：加字体 = 往 `Fonts/` 加数据 + 注册一行；Settings / Gui **不**直接 `#include` 某份点阵表。

```
Fonts/                         ← 字体资源 + 注册表（与 Gui/Settings 解耦）
  Font.h / FontRegistry.c
  terminus16x32.c  (+ 第二套可切换字体)
HAL/.../Video.c  ──► Font_*    ← 不再直接 include FontData.h
Theme / Desktop / Settings
ToyBoot GOP ←── THEME.CFG / MODE.CFG（分辨率：下次启动生效）
```

### 分辨率说明（能否在 Settings 里改？）

**可以纳入 Settings**，但语义要分清：

| 方式 | 可行性 | 说明 |
|------|--------|------|
| **选模式 → 写盘 → 重启后生效** | ✅ 推荐（PR-D7） | 现分辨率由 **ToyBoot** 经 UEFI **GOP `SetMode`** 选定，再把 FB 交给内核。`ExitBootServices` 后内核无 GOP。Settings 写偏好（如 `800x600`），ToyBoot 启动时读配置并匹配 GOP 模式表 |
| **运行中热切换** | ⚠ 后置 | 需内核侧 virtio-gpu / Bochs VBE 等模式设置；工作量大，不放进 3b 主线 |
| **仅逻辑缩放（同 FB 画小桌面）** | 可选教学 | 非真改分辨率；可作 D7 之外的实验，不替代 GOP |

预置可选列表（与 ToyBoot 虚拟机友好表对齐即可）：如 `800×600` / `1024×768` / `1280×720` / `1600×900`；无匹配模式则保持当前并提示。

### 建议 PR 顺序

| PR | 主题 | 主要文件 | 目标 | 状态 |
|----|------|----------|------|------|
| **PR-D1** | **字体子系统** | `Fonts/`、`Video.c`、`HalVideo` | `FONT_FACE` API；Terminus 为 `font0`；换字不改 Gui | ✅ |
| **PR-D2** | **主题存储** | `Theme.c/h`、`Gui.c` | `DesktopBg` / `ShellClientBg` / `FontId`；重绘读主题 | ✅ |
| **PR-D3** | **窗口种类 + 打开 API** | `Gui.c` | `GUI_WIN_SHELL` / `SETTINGS`；`GuiOpenShell()`；启动默认可无应用窗；`MAX_WINS` 略增 | ⬜ |
| **PR-D4** | **桌面图标 + 双击** | `Desktop.c`、`Gui.c` | 图标表（Shell / Settings）；双击检测；空白不误开 | ⬜ |
| **PR-D5** | **Settings 程序** | `SettingsUi.c`、`Theme` | 切字体、选背景色；即时 `GuiRedraw`；换字后清客户区避免行高错位 | ⬜ |
| **PR-D6** | **（可选）主题持久化** | `Theme` + FAT | `THEME.CFG`；启动 `ThemeLoad` | ⬜ |
| **PR-D7** | **Settings 改分辨率** | `SettingsUi`、`Theme`/`DisplayCfg`、**ToyBoot** | Settings 选 WxH → 写配置；提示「重启后生效」；ToyBoot 读配置 `GOP SetMode`；内核仍只消费 FB | ⬜ |

**节奏**：先合 D1+D2（底座）→ D3+D4（双击开 Shell）→ D5（Settings 字体/背景）→ D6 持久化 → **D7 分辨率（可与 D6 合并写同一 CFG）**。

#### PR-D1 验证

- 启动 / Shell 文字与现网一致；`clear`、滚动行高正确
- `rg FontData Common` 无业务直接依赖（仅 `Fonts/` 持有字形）

#### PR-D2 验证

- 默认观感与改前一致（桌面深灰、Shell 客户区浅灰、Terminus）
- `GuiRedraw` / 关窗露底 / 拖动合成读 `ThemeDesktopBg()`；Shell 初始 `Background = ThemeShellClientBg()`
- 代码侧：`ThemeSetDesktopBg(COLOR_BLUE); ThemeApply();` 后桌面变蓝（D5 前可用临时改 `ThemeInit` 默认值重编确认）
- `ThemeSetFontId` 越界返回 -1；合法 Id 与 `FontSetById` 一致

#### PR-D4 验证

```
# 启动见桌面图标（非强制全屏 Shell）
# 双击 Shell → 开 ToyOS Shell 窗，可输入 help
# 单击空白不打开；窗可拖、可关回桌面
```

#### PR-D5 验证

```
# 双击 Settings → 选背景色 / 下一字体
# 桌面立即变色；Shell 标题与正文用新字体（已开窗建议清客户区）
```

#### PR-D7 验证

```
# Settings 选 1024x768 → 保存 → 提示重启
# 重启后串口/日志为 ToyBoot: display 1024x768 …；桌面与窗口按新尺寸布局
# 选无效模式时保持原分辨率且有提示
```

**本阶段不做**：图标拖放排序、开始菜单、壁纸位图、TTF、多语言、用户态 GUI toolkit、**运行中 GOP/热切换分辨率**。图标首版只开内核窗（Shell/Settings）；绑 `exec HELLO.ELF` 可后置。

---

## 阶段 4：网络栈 ≈ 进行中

### 已完成能力

| 组件 | 文件 | 内容 |
|------|------|------|
| virtio-net | `Net.c` | ARP、ICMP、`NetSendIp`；入站 IP 学 ARP |
| 自研 UDP/TCP | `Udp.c` / `Tcp.c` | UDP 收发；TCP 发送缓冲 + 对端窗口 + 超时重传 + echo |
| lwIP 可选栈 | `ThirdParty/lwip` + `HAL/X86_64/LwIp/` | `make LWIP=1`；`toy_netif` → virtio 以太网帧 |
| lwIP ping/echo/UDP | `toy_ping.c` / `toy_tcpecho.c` / `toy_udp.c` | `lwip on` 后 `ping` / `tcplisten` / `udpsend` / `udplisten` 走 lwIP |
| listen UX | `Console.c` / `ShellCommands.c` | 抑制 `toyos>` 与空行；`Ctrl+C` / `tcplisten stop` |

**验证（builtin，默认 `make`）**：
```
toyos> ping 10.0.2.2
toyos> tcplisten 9000          # 宿主机 nc 127.0.0.1 9000
toyos> tcpstatus               # snd_una / nxt / peer_wnd
```
`tcpconnect` 占用唯一 TCP 槽，勿与 `tcplisten` 同测；连宿主机请 `nc -l <port>` 后 `tcpconnect 10.0.2.2 <port> ...`（勿用 80）。

**验证（lwIP，`make LWIP=1`）**：
```
toyos> lwip on
toyos> ping 10.0.2.2
toyos> tcpconnect 10.0.2.2 8888 hi   # 宿主机先 nc -l 8888
toyos> tcplisten 9000          # Ctrl+C 或 tcplisten stop 结束
toyos> udplisten 9999          # 宿主机 echo -n hi | nc -u 10.0.2.15 9999
toyos> udpsend 10.0.2.2 9999 hello   # 宿主机 nc -u -l 9999
```

`run.sh` / `run-split.sh` 已 `hostfwd=tcp::9000-:9000`。

### 阶段 4 下一步（建议 PR 顺序）

| PR | 主题 | 主要文件 | 目标 | 状态 |
|----|------|----------|------|------|
| **PR-N1** | **lwIP 脚手架** | `LwIp.c`、`toy_netif.c`、`lwipopts.h`、`Makefile` | `make LWIP=1`；`lwip on` 接管 RX | ✅ |
| **PR-N2** | **lwIP ping + echo** | `toy_ping.c`、`toy_tcpecho.c`、`ShellCommands.c` | ICMP raw + TCP accept/echo；Shell 路由 | ✅ |
| **PR-N3** | **listen 模式 UX** | `Console.c`、`Tasks.c`、`Tcp.c` | 无多余空行/提示符；`Ctrl+C` / `stop` | ✅ |
| **PR-N4** | **lwIP UDP** | `toy_udp.c`（新）、`LwIp.c`、`ShellCommands.c` | `lwip on` 时 `udpsend` / 入站 UDP 回调走 lwIP | ✅ |
| **PR-N5** | **lwIP 主动连接** | `toy_tcpclient.c`（新）、`ShellCommands.c` | `lwip on` 时 `tcpconnect` 走 lwIP；可与 listen 并存 | ✅ |
| **PR-N6** | **RX 栈统一** | `Net.c`、`LwIp.c`、`Tasks.c`、`ShellCommands.c` | `lwip on` 后 RX/TX 仅 lwIP；builtin 禁用；文档双栈策略 | ✅ |
| **PR-N7** | **自研 TCP 冻结为 legacy** | `Tcp.c`、`ThirdParty/README.md` | 不深化多连接/拥塞；文档双栈；主栈 = lwIP | ✅ 文档化 |
| **PR-N8** | **用户态 socket** | `Syscall.c`、`toy_socket.c`、`netdemo`/`netsrv` | `socket`/`connect`/`bind`/`listen`/`accept` + fd I/O | ✅ |

#### PR-N8 验证（需 `./build.sh LWIP=1`）

```bash
# 客户端（宿主机先 nc -l 8888）
toyos> exec NETDEMO.ELF

# 服务端（ToyOS listen :9000；看到 listen:9000 后再连）
toyos> exec NETSRV.ELF
# 另一终端：
echo hi | nc 127.0.0.1 9000
# ToyOS 应打印 ok；nc 应看到 hi
```

ABI：
| 调用 | 号 | 参数 |
|------|-----|------|
| `socket` | 8 | AF_INET / SOCK_STREAM |
| `connect` | 9 | fd, ip host-u32, port |
| `bind` | 10 | fd, ip(`0`=ANY), port |
| `listen` | 11 | fd, backlog |
| `accept` | 12 | listen_fd → 新 fd |
| `write`/`read`/`close` | 1/3/4 | socket fd 上即 send/recv |

详见 `Include/Socket.h`。

#### PR-N4 验证

```
toyos> lwip on
toyos> udpsend 10.0.2.2 9999 hello   # 宿主机 nc -u -l 9999
# 入站 UDP 打印到 Shell（与现 builtin 行为一致）
```

#### PR-N5 验证

**先测单独连接（推荐）**：

```
# 终端 1（宿主机，必须先启动）
nc -l 8888

# ToyOS
toyos> lwip on
toyos> tcpconnect 10.0.2.2 8888 hi
tcpconnect: done          # 终端 1 应看到 hi
```

**再测与 tcplisten 并行**（lwIP 多 PCB，需开第二个 Shell 窗，体验更清晰）：

```
# 终端 1：nc -l 8888
# 终端 2：nc 127.0.0.1 9000

toyos> lwip on
toyos> tcplisten 9000                    # 无 toyos> 提示符，正常
# 点击 + 开 Shell 2，或 Ctrl+C 停止 listen 后再连
toyos> tcpconnect 10.0.2.2 8888 hi
```

**常见误判**：

| 现象 | 原因 |
|------|------|
| `syn failed` | 宿主机未 `nc -l 8888`，连接被拒绝 |
| `lwip: echo server stopped` | 按了 **Ctrl+C**（会优先停 tcplisten，不是 tcpconnect 失败导致） |
| `timeout` + virtio 报错 | 前次失败后网卡状态异常；重启 QEMU，并确保 host 已 listen |
| listen 期间同窗输入 | 可以执行，但无 `toyos>`，易与 echo 输出混淆；建议用 Shell 2 |

#### PR-N6 要点（已实现）

- **`lwip on` = 生产路径**：RX 全走 `ToyNetifInput`；`NetSendIp` / `NetPing` 在 `gLwIpRx` 时拒绝。
- **`LwIpInit`**：重置 builtin `Tcp`/`Udp`；`Tasks` 在 lwIP 活跃时跳过 `TcpPoll`。
- **`net` / `lwip status` / `tcpstatus`**：显示当前栈（builtin vs lwIP unified）。
- 默认 `make`（无 lwIP）行为不变。

---

## 阶段 5：多核 SMP

### 架构原则：Common 一套，HAL 分家

**是的——不同架构共用同一套 `Common`（调度、锁、进程）。**  
多核差异（x86 的 MADT/SIPI/LAPIC，ARM 的 PSCI/GIC，RISC-V 的 SBI/PLIC）全部收在 **`HAL/<Arch>/`**，经 `Hal.h` 门面暴露：

| Common 只看见 | x86 实现 | Arm64 / RiscV（后置） |
|---------------|----------|------------------------|
| `HalCpuCount()` / `HalCpuId()` | MADT Local APIC / LAPIC ID | PSCI / hart id |
| `HalSmpStartAps()` | INIT+SIPI + trampoline | `cpu_on` 等 |
| `HalIpiSend()`（S2+） | LAPIC ICR | GIC SGI / SBI |
| 自旋锁、`SchedulerOnTimer` | 架构无关 | 同左 |

Common **不** `#include` MADT/LAPIC；AP 起来后进同一套 `Scheduler` / idle。

### 建议 PR 顺序

| PR | 主题 | 主要文件 | 目标 | 状态 |
|----|------|----------|------|------|
| **PR-S1** | **发现 + 拉起 AP** | `AcpiMadt.c`、`SmpBoot.c`、`SmpTrampoline.S`、`Startup.c`、`run.sh` | 解析 MADT；SIPI；AP 串口 `smp: hello`；`-smp 2` | ✅ |
| **PR-S2** | **每核定时器 + CpuId** | `Arch.c`、`SmpBoot.c`、`Hal.h` | 每核 LAPIC timer；逻辑 `HalCpuId`；AP 计 tick 不调度 | ✅ |
| **PR-S3** | **锁 + 调度多核安全** | `Include/SpinLock.h`、`Scheduler.c`、热点全局 | 自旋锁；共享 runqueue + 大锁或每核队列雏形；无撕账 | ✅ |
| **PR-S4** | **负载均衡** | `Arch.c` TSS、`Scheduler.c` | 每核 TSS；每核 READY 队列 + 偷任务；用户态可上 AP | ✅ |

#### PR-S1 验证

```bash
# run.sh 含 -smp 2（或更多）
cd ToyKernel && ./build.sh
cd ../ToyImage && ./run.sh
# 串口应类似（默认构建即可，不依赖 DEBUG=1）：
# smp: MADT cpus=0x00000002 bsp_apic=0x00000000
# smp: hello cpu apic=0x00000001
# smp: APs started=0x00000001 hellos=0x00000001
```

**PR-S1 已实现要点**：RSDP→MADT；INIT/SIPI + trampoline；AP hello 后 `hlt`；保留 `0x7000–0x8FFF`；Arm64/RiscV stub 单核。

#### PR-S2 验证

```bash
cd ToyKernel && ./build.sh && cd ../ToyImage && ./run.sh
# 串口应有：
# smp: hello cpu=0x00000001 apic=0x00000001
# smp: ticks cpu0=0x0 cpu1=<非零>   # 此时 BSP 尚未 TimerStart，cpu0 可为 0
```

**PR-S2 已实现要点**：逻辑 `HalCpuId`（0=BSP）；`ArchApInit` 修正 trampoline GDT 的 0x08 后共享 IDT；AP `TimerStart`+`sti`；`HalCpuTickInc`；AP 不进 `SchedulerOnTimer`。启动时对 AP tick 短轮询（避免超长 `DelayLoops` 卡住、GUI 出不来）。

#### PR-S3 验证

```bash
cd ToyKernel && ./build.sh && cd ../ToyImage && ./run-split.sh
# 串口在 smp: ticks 之后应能到 fs / ToyOS console，且有 Shell 窗
# DEBUG=1 时可见：sched: AP entered idle cpu=0x1
# 双核：exec FORK.ELF / 开窗拖动不崩；ps 可见 idle0/idle1
```

**PR-S3 已实现要点**：`SpinLock.h`；调度大锁；每核 `gCurrentCpu` + `idleN`；`SchedulerApStart`；共享 READY 队列；`shell`/`gui`/用户态 Affinity=BSP（单 TSS）；`worker` 可被 AP 调度。

#### PR-S4 验证

```bash
cd ToyKernel && ./build.sh && cd ../ToyImage && ./run-split.sh
# Shell 正常；ps 可见 idle0/idle1、steals=…、任务 home/cpu 字段
# exec FORK.ELF / COUNT.ELF 不崩；worker 可出现在 AP（cpu=1）
```

**PR-S4 已实现要点**：共享 GDT 上每核 TSS + `ArchSetRsp0`；AP `lgdt`/`ltr`；每核 READY 队列；空闲时从其它核队尾偷；`shell`/`gui` 仍 Affinity=BSP；用户态/worker 可迁移。

### 仍后置

- 真机 IOAPIC / 中断路由细化  
- Arm64 / RISC-V 真实 `HalSmp*`（非 stub）

---

## 架构重构 ✅ PR-1～4 已完成

| PR | 内容 | 要点 |
|----|------|------|
| PR-1 | 目录布局 | `Common/{Core,Services,Library}`、`HAL/<Arch>/Drivers/` |
| PR-2 | 公共头文件 | API 收拢到 `Include/`；`Hal.h` 统一 HAL 入口 |
| PR-3 | Boot 解耦 | `BOOT_INFO` + `KernelMain()`；UEFI `BOOT_CONFIG` 仅在 `HAL/X86_64/Startup.c` |
| PR-4 | HAL 设备门面 | `BlockRegisterBackend` / `HalBlockInit`；`HalDevices.h`（USB 输入、virtio-net） |

**附带**：`Kernel.c` 拆为 `KernelModules` / `ShellCommands` / `Tasks`；`exec` 完成后才刷新 `toyos>`。

**验证**：`./build.sh` + `ToyImage/run.sh` → `exec FORK.ELF`（P/C/done，无屏幕调试杂讯；`DEBUG=1` 时串口可见 exit 日志）

**状态**：`refactor/pr4-hal-layering` 已合并进 `main`。

### PR-5：HalVideo / HalSerial ✅ 已完成

**目标**：Common / Services 不再 `#include` `HAL/<Arch>/Drivers/Video.h` 或 `Serial.h`；Makefile 中 Common 编译不再加入 `HAL/<Arch>/Drivers` 的 `-I`（驱动头仅 HAL 侧可见）。

**现状（待消除）**：

| 头文件 | Common 直接引用方 | 状态 |
|--------|-------------------|------|
| `Serial.h` | ~~Console / Tasks / Module / KernelModules / Tcp / Udp / Gui~~ | ✅ PR-5a 已改 `HalSerial.h` |
| `Video.h` | ~~`Console.c`（绘制）、`Gui.c`、`UI.c`、`KernelModules.c`（`VideoClearScreen`）~~ | ✅ PR-5c 已改 `HalVideo.h` |

`Hal.h` 已有 `HalConsole` / `HalDebugWrite*`；Common 经 HAL 门面访问串口与帧缓冲，不再 include 驱动头。

#### 建议 PR 顺序

| PR | 主题 | 主要改动 | 验证 |
|----|------|----------|------|
| **PR-5a** | **HalSerial 门面** | `Include/HalSerial.h`；`HAL/X86_64/HalSerial.c` 委托 `Serial.c`；Common 改调 HAL；`Serial.h` 仅 HAL 内部 | ✅ `./build.sh`；Shell 与 `DEBUG=1` 串口日志 |
| **PR-5b** | **HalVideo 初始化** | `Include/HalVideo.h`：`HalVideoSet` / `HalVideoGetSize`；`Kernel.c`/`KernelModules` 经 HAL 挂帧缓冲；`VIDEO_CONFIG` 已在 `BootInfo.h` | ✅ 启动分辨率正确；`ConsoleWrite` 不因 Width=0 死循环 |
| **PR-5c** | **HalVideo 绘制与 clip** | 门面封装：`DrawPixel`/`FillRect`/`CopyRect`/`ReadRect`/`WriteRect`、`DrawChar(At)`/`DrawString(At)`、文本光标、`SetClipRegion`/`ClearClip`；`Console`/`Gui`/`UI` 改 include `HalVideo.h` | ✅ 双窗 Shell、拖动、`clear`、滚动 |
| **PR-5d** | **构建与头文件收口** | Makefile：`CFLAGS_COMMON` 不含 `Drivers`；`CFLAGS_HAL` 含驱动 `-I`；`Video.h`/`Serial.h` 为 HAL 私有 | ✅ `rg 'Drivers/' Common` 无匹配；`ARCH=riscv` 占位 HAL 可编 |
| **PR-5e** | **（可选）Console 统一** | `Include/HalConsole.h`；`Console.c` 经 `HalConsole*` 输出；`HalConsolePutChar` 与 `HalSerialWrite` 职责文档化 | ✅ 模块 Init 顺序不变；Video 未就绪时仅串口 |

### PR-6：HAL 去 x86 命名 ✅ 已完成

| 旧名 | 新名 |
|------|------|
| `HalIdtSetGate` | `HalIrqVectorSet` |
| `HalPagePrivatizePml4Slot` | `HalPagePrivatizeRootSlot` |
| `VirtualMemoryKernelCr3` / `LoadCr3` / `SpaceCr3` | `VirtualMemoryKernelRoot` / `LoadPageTable` / `SpaceRoot` |
| `TASK.Cr3` | `TASK.PageRoot` |

x86 实现内仍可用 `%%cr3` / `Pml4` / `ArchIdt*`；Common 与 `Hal.h` 对外为架构中性命名。

**验证**：`./build.sh`；Arm64/RiscV stub `Hal.c` 可单独编译。

#### 仍后置

- 用户态 ABI（`int 0x80`、`EM_X86_64`、`user.ld`）→ 按目标架构单独 bring-up
- RISC-V / ARM64 真实驱动与 Boot（非 stub）

---

## 建议的下一步（按优先级拆分）

### A. 桌面（阶段 3b）— 当前主线

| 顺序 | 项 | 说明 |
|------|-----|------|
| 1 | ~~**PR-D1** 字体子系统~~ | ✅ `Fonts/` + `Font_*` API；与 Gui 解耦 |
| 2 | ~~**PR-D2** 主题存储~~ | ✅ `Theme`：`DesktopBg` / `ShellClientBg` / `FontId` + `ThemeApply` |
| 3 | **PR-D3** 窗口种类 + Open API | Shell/Settings 种类；启动可空桌面 |
| 4 | **PR-D4** 桌面图标 + 双击 | 双击开 Shell（及 Settings 入口） |
| 5 | **PR-D5** Settings | 切字体、背景色 |
| 6 | PR-D6 主题写盘 | 可选；`THEME.CFG` |
| 7 | **PR-D7** Settings 改分辨率 | 写偏好 + **ToyBoot GOP**；**重启后生效**（非热切换） |

### B. 进程与存储（可穿插）

| 顺序 | 项 | 说明 |
|------|-----|------|
| 1 | ~~COW fork~~ | ✅ 写时复制；`exec FORK.ELF` |
| 2 | ~~`WNOHANG`~~ | ✅ `wait(rdi=WNOHANG)`；`exec WAITNH.ELF` |
| 3 | ~~动态库 `.so`~~ | ✅ `DT_NEEDED` + `JUMP_SLOT`；`exec DYNDEMO.ELF` |
| 4 | ~~FAT LFN~~ | ✅ 读/写/列目录；`write longfilename.txt` |
| 5 | ~~**PR-SC1** `syscall`/`sysret`~~ | ✅ 与 `int 0x80` 解耦；`SYSHELLO`/`SYSFORK`；legacy 保留 |
| 6 | `execve` / 管道或简单信号 | 教学向进程扩展（择一先做） |
| 7 | NVMe/AHCI / virtio-blk / 安装器 | 真机读写盘时再急 |

### C. 网络（阶段 4 收尾，可穿插）

| 顺序 | 项 | 说明 |
|------|-----|------|
| 1 | ~~PR-N4～N8~~ | ✅ lwIP UDP/connect/RX 统一/socket |
| 2 | 默认 `LWIP=1` 或文档强化 | 生产路径更明显；builtin 保持 legacy |
| 3 | DNS / 更完整 socket 错误码 | 按需 |

### D. GUI 抛光与架构（低优先级）

| 项 | 说明 |
|-----|------|
| ~~PR-G1～G5~~ | ✅ 关窗/双终端/拖动/闪屏 |
| `GuiTask` / `ShellTask` 分工 | 可选 |
| 光标残影等边角 | 拖动后 save-under 偶发残留 |
| 接入 `UI.c` 控件 | Settings/桌面图标可复用按钮 |
| ~~PR-6 HAL 命名~~ | ✅；跨架构 ABI / 真 stub 仍后置 |

### E. 多核（阶段 5 收尾）

| 顺序 | 项 | 说明 |
|------|-----|------|
| 1 | ~~PR-S1～S4~~ | ✅ MADT/SIPI、timer、大锁、每核队列偷任务 |
| 2 | 减弱调度大锁 / 优先级 | 可选 |
| 3 | `shell`/`gui` 可迁移 | 需输入同步；现钉 BSP |
| 4 | 真机 IOAPIC / 中断路由 | 后置 |
| 5 | Arm64 / RiscV 真 `HalSmp*` | 后置 |

### F. 各模块仍待改进（总览）

| 模块 | 待改进 |
|------|--------|
| 进程/系统调用 | 任务槽约 8；无 `execve`/`mmap`/`brk`/信号/管道；FD 整文件缓冲；`.so` 仅 JUMP_SLOT；syscall 用户库封装可选 |
| 存储 | 仍 ATA PIO；无 NVMe/AHCI/virtio-blk；FAT 写 ≤64K；无安装器 |
| GUI | 桌面/设置见 3b；`UI.c` 未全面接入；无 TTF/完整 Unicode |
| 网络 | builtin TCP legacy；lwIP 无 DNS；用户 socket 需 `LWIP=1` |
| SMP | shell/gui 钉 BSP；大锁偏粗；无公平调度/热插拔 |
| 内存 | 无用户堆 API、无 swap/页缓存/VFS |
| 跨架构 | RiscV/Arm64 仅为 stub |

### 已完成里程碑（归档）

- ~~PR-1～4 架构重构~~ → `main` ✅
- ~~PR-5 HalVideo / HalSerial~~ ✅
- ~~PR-6 HAL 命名去 x86~~ ✅（`HalIrqVector*`、`PageRoot`、`PrivatizeRootSlot`）
- ~~PR-G1～G5 GUI 收尾~~ ✅
- ~~PR-N1～N6 lwIP 脚手架 … RX 栈统一~~ ✅
- ~~PR-N7 自研 TCP 冻结为 legacy + 双栈文档~~ ✅
- ~~PR-N8 用户态 socket（lwIP）~~ ✅
- ~~COW fork~~ ✅
- ~~WNOHANG~~ ✅
- ~~动态库 .so~~ ✅
- ~~FAT LFN~~ ✅
- ~~PR-SC1 syscall/sysret~~ ✅（`int 0x80` legacy 保留）
- ~~PR-S1～S3 SMP~~ ✅（MADT/SIPI、每核 timer、调度大锁 + AP idle）
- ~~PR-S4 负载均衡~~ ✅（每核 TSS、每核队列、偷任务）
---

## QEMU 备忘

```bash
cd ToyKernel && ./build.sh              # 默认（builtin 网络）
cd ToyKernel && ./build.sh LWIP=1       # 含 lwIP ping/echo
cd ToyKernel && DEBUG=1 ./build.sh      # 串口调试日志
cd ../ToyBoot && ./build.sh             # 更新 BOOTX64.EFI
cd ../ToyImage && ./run.sh              # 单盘
cd ../ToyImage && ./run-split.sh        # 双盘；--force-second 强制第二盘内核
```
