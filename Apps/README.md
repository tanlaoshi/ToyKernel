# Apps/ — 已安装应用（PR-S0 占位）

Guest 卷根：`Apps/`。

- **S0**：仅约定目录；可空。  
- **S1**：商店/优盘安装将用户 ELF 复制到此（如 `Apps/HELLO.ELF`）。  
- 启动菜单 / `exec` 仍可用卷根现有演示 ELF；不强制迁走。

勿把手动构建垃圾文件提交进本目录。详见 `Assets/Store/README.md`。
